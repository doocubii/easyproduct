# CLAUDE.md — 이 저장소에서 작업할 때

easyproduct는 비개발자가 대화만으로 개발 착수 전 문서 세트를 만들게 돕는 Claude Skills 모음이다.
각 스킬은 `skills/easyproduct-*/`에 있고, 산출 문서는 **사람이 읽는 층 + 소프트웨어가 읽는 기계 층**
(frontmatter + 태그된 ` ```json ` 블록 + 옆의 JSON Schema)을 함께 담는다.

## 기계 블록을 설계·수정할 때 — 핵심 원칙 (빈 껍데기 금지)

문서에 기계 블록을 새로 만들거나 고칠 때, **그 블록의 존재 이유인 필드를 비어 있게 두지 말 것.**

- **스키마에서 필수로 강제한다.** 블록의 실제 값을 담는 필드(참조 목록·항목 목록 등)는 `required`에
  넣고, 배열이면 `minItems: 1`을 건다. 식별자(`id`)만 있고 내용이 빈 엔트리가 스키마를 통과하면 안 된다.
  (예: 화면 블록의 `components`, 시나리오의 `refs` — 둘 다 필수·최소 1개.)
- **스킬 지침에서 "산문을 빠짐없이 미러하라"를 명시한다.** "블록을 넣는다"로 끝내지 말고, 산문의 어느
  부분(예: 화면 "보이는 것"의 모든 `UI.*`, "데이터 정의"의 모든 변수)을 그 필드에 **전부** 담고 **비우지
  말라**고 못박는다.
- **왜:** 이 내용 필드들이 크로스도큐먼트 참조 점검의 근거다. 비면 점검할 게 없어 "있으나마나한 블록"이
  된다 (2026-07 실제 결함: 화면 엔트리가 `{id, feat}`만 담고 컴포넌트·데이터를 전부 버린 사례).

**판단 기준:** "이 엔트리에서 식별자를 빼면 남는 게 있는가?" 없으면 무의미하다 — 내용 필드를 필수화하라.

- **"레지스트리라 ID만 있어도 된다"는 합리화를 경계하라.** id는 대개 산문 헤딩(`## UI.button.primary`)과 중복이고, `scope` 같은 값은 id에서 유도된다. 그런 블록은 grep 결과를 복제한 것이라 **산문 파싱을 대신하지 못한다**(블록의 목적 실패). 목록/레지스트리 블록도 그 대상의 **정의·속성**(예: 컴포넌트의 규격·파라미터·variants, 규칙의 도메인, 기능의 라벨·상태)을 담아 "id를 빼도 내용이 남게" 한다. (2026-07 `uicomponents.list`가 `{id, scope}`만 담아 무의미했던 사례.)

## 점검 방법 원칙 — 스킬을 만들 때 관철하고, 베타 머지 때 부합을 강제한다

이 세트의 점검은 **3층**이다(누가·무엇을·언제). 축은 「의미판단 vs 결정론」:

- **소프트웨어(점검기, 아무때나)** — ① ID 형식 ② 산문↔블록 매칭(coverage) ③ 블록 유효성(스키마·크로스참조).
- **LLM(에이전트, 풀 리뷰 때만)** — ④ 산문 충실도 ⑤ 미러 충실도(correctness).
- **풀 리뷰 시기**: 사용자 요구 / 문서 최초 완성 시 / 문서 수정 후(부분 풀 리뷰=수정 항목+파장) / 에이전트 판단.

**본문·근거·부합 체크리스트는 `skills/easyproduct-suite/references/checker-guide.md`에 있다(배포되는 정본).
여기서 재서술하지 말고 그 문서를 따른다.**

- **스킬을 만들거나 고칠 때:** 그 스킬의 "정합성 점검" 절을 위 3층·시기·수정 방향과 **정확히 일치**시킨다.
  기계 블록을 내는 스킬이면 checker-guide의 부합 체크리스트(빈 껍데기 금지·뜻 필수·충실 미러 지침 등)를 만족시킨다.
- **`beta` 머지/버전업 시 — 스킬 부합 게이트를 반드시 실행한다.** checker-guide의 "스킬 부합 점검" 체크리스트로
  스킬들이 원칙대로 만들어졌는지 점검하고, **하나라도 실패하면 버전업/머지를 막고 먼저 스킬을 고친다.**
  (기계 가능 항목은 스킬-린트 `skills/easyproduct-suite/scripts/skill-lint.mjs`로, 의미 판단 항목은 에이전트가.
  VERSIONING의 베타 체크리스트에 필수 항목으로 있다.)

## 규약·근거 문서 (재파생하지 말고 이 문서들을 따를 것)

- **기계 층 계약** — frontmatter 필드 의미·`doc_type`/anchor 레지스트리·참조 라우팅·점검 알고리즘:
  `skills/easyproduct-suite/references/checker-guide.md`. 참조 점검기: `skills/easyproduct-suite/scripts/check-docs.mjs`
  (무의존 Node, suite가 실행. 프로젝트로 복사 안 함, 소스는 요청 시 제공).
- **버전·릴리즈 규칙**: `VERSIONING.md`. 세트 전체가 한 버전을 공유하고, 버전은 `beta` 머지 시 확정한다.
  **공개·자기보고 표기(루트 `README.md`·각 `SKILL.md` 메타)에는 스테이지(beta/main) 라벨·문구를 넣지 않는다 —
  버전·날짜 값만.** 스테이지·갱신 규칙은 `VERSIONING.md`에만.
- **브랜치**: 작업은 `beta`에서 분기(`feature/*`), 완료 시 `beta`로 머지. 커밋은 자유, **푸시는 확인 후.**
- **산출 문서 원칙**: 비전문가 언어, 초안 우선(화면 설계서만 처음부터 확정 명세), 항상 파일로.
  스킬 간 참조는 최소화 — 각 스킬은 독립 동작하며 다른 스킬의 존재를 전제하지 않는다.
  세부는 각 스킬 `SKILL.md`를 따른다.
