# 변경 내역 (Changelog)

이 저장소(easyproduct 스킬 세트)의 버전별 변경 내역이다. 버전·날짜를 언제 확정하는지는 `VERSIONING.md`를 따른다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/) 관례를 참고하되 한국어로 정리한다.

- **버전 번호**는 작업 브랜치가 `beta`에 머지될 때 확정된다.
- **릴리즈 날짜**는 단일 필드로, 머지될 때마다 기록된다 — `beta` 머지 시 beta 머지일, `main` 머지 시 그 날짜로 갱신.

---

## [0.12.0] — 2026-08-28 (beta)

> 상태: `beta` 머지로 **버전 0.12.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-28).
> 근거: GitHub 이슈 #3 **3차분**(항목 5·6). **MINOR인 이유**: 새 산출물(`interface-revisions.json`)과
> 새 명령 둘이 생겼고, 기존 세트는 **한 번 돌려야** 그 자리가 생긴다(이행이 있다).

### Added — 인터페이스 **항목 단위 개정** (`interface-revisions.json`)

계약 문서 하나에 인터페이스가 20개 넘게 사는데 `revision`이 문서 하나에 하나뿐이라, 소비자가
**"내가 맞춘 판이 아직 유효한가"**를 물을 자리가 없었다.

- **스크립트가 계산한다.** `--sync-revisions`가 계약 내용의 해시로 개정을 계산해 기록한다.
  `--check-revisions`는 **대조만** 하고 어긋나면 종료코드 1(CI용). 문서 세트 밖에서도 부를 수 있다.
- **사람이 적게 하면 빈다** — 실측: 계약 130건 중 **117건(90%)**이 근거를 한 줄만 적었고, 그걸 근거로
  만든 검사의 "미해결 9건"이 **전부 오탐**이었다. 그래서 사람은 **더 올릴 수만** 있다:
  계약 문서의 `interfaces[].revision`은 **필드는 그대로인데 뜻이 바뀐 경우**(기계가 못 보는 변경)에만 적고,
  **계산값보다 작게 적으면 오류**다(개정은 내려가지 않는다).
- **계약 내용만 센다** — `basis`·`notes`를 고쳐도 개정은 오르지 않는다. 계약이 바뀐 게 아니다.
- **축을 섞지 않는다고 스키마에 못박았다** — `version`(payload 스키마 계약 버전) · 문서 `revision` ·
  `interfaces[].revision`. 실제로 이 축이 뒤섞여 v1 스키마 문서에 `version: 13`이 박힌 사고가 있었다.
- **사람이 읽는 층은 조용하다** — 개정 번호는 산문에도 블록에도 안 실린다(별도 파일). "항목 단위로 두면
  사람이 읽을 때 시끄럽다"는 우려가 그대로 해결된다.

### Added — 재생성이 **무엇을 지우는지** 알린다

요청서는 **파생물**이라 통째로 다시 만들어지고, 계약 문서는 **손질**하는 곳이라 성질이 반대다.
같은 습관으로 요청서에 적으면 **요청서 쪽에서만 조용히 샌다**(실사용 제보: 스키마에 없는 키를 손으로
넣었는데 다음 생성 때 사라졌다).

- `--emit-interface-request`가 기존 파일에만 있는 것을 **지우기 전에 알린다** — 손으로 넣은 키와,
  화면에서 없어졌거나 `target`이 서버가 아니게 된 **요구 자체**.
- **남겨야 할 내용이면 원본(화면 설계서)에 적고 다시 뽑으라고** 안내한다.

### 업그레이드 경로

- **감지** — suite Step 1 공통 gap-fill에 "개정 기록이 없다" 추가.
- **채움** — `--sync-revisions` 한 번. 전부 **개정 1**로 시작하고 **과거 이력은 지어내지 않는다**
  (문서 `revision` 도입 때와 같은 규칙).
- **가시화** — 기록이 없으면 알리고, 있는데 어긋나면 건별로 집계한다(무엇이 몇 → 몇으로).
- **검증** — 실사용 세트(인터페이스 130)로 왕복: 기록 없음 → sync → 일치 → 계약 변경 시 오름 →
  **근거만 바꾸면 안 오름**까지 확인.

### 회귀

165건(+13). 옛 코드에 새 회귀를 돌려 **14건 실패** 확인.

---

## [0.11.3] — 2026-08-28 (beta)

> 상태: `beta` 머지로 **버전 0.11.3 확정**, 릴리즈 날짜 = beta 머지일(2026-08-28).
> 근거: GitHub 이슈 #3 **2차분**(항목 1·2·4). 새 칸은 전부 **선택**, 새 검사는 전부 **경고**다.

### Added — 목록의 **항목 모양** (`items`)

`type: "list"`에서 멈추면 받는 쪽은 원소가 무엇인지 알 수 없었다. 실측 사고: 원소 모양이 없어
프론트가 객체 배열을 보냈고 서버는 문자열 배열을 기다려 **422**가 났다(하루 소요).

- 계약(`request.fields[]`·`response.fields[]`)과 **데이터 모델**(`fields[]`) 양쪽에 `items` 칸.
  원소가 객체면 `items.fields`, 열거형이면 `items.values`, 목록의 목록이면 `items.items`.
  데이터 모델은 원소가 다른 그룹의 덩어리일 때 `items.group`으로 가리킨다.
- **고칠 자리를 갈라 안내한다** — 원소의 필드를 `이름[].속성`으로 **펼쳐 적은 것**(실측 38건)은
  형제 배열이라 스키마가 못 본다. 그건 **모으는 일**이지 새로 적는 일이 아니다(실측 33건은 새로 적을 것).

### Added — 값의 **근거 갈래** (`derivedFrom` · `transient`)

`dataModel` 하나뿐이라 **저장된 값 · 파생 · 일회성**이 구별되지 않았다(실측 990필드 중 317건이 비어 있었고,
그 안에 일회성 165 · 파생 28 · 컨테이너 45가 섞여 있었다).

- **셋 중 하나로 적는다** — `dataModel`(저장) · `derivedFrom`(파생: `from[]` 최소 1 + `how` 한 줄) ·
  `transient: true`(저장 안 함). **둘을 동시에 적으면 오류**, 셋 다 없으면 "미기재"로 집계(실측 276건).
- **파생 출처는 등기부 대조를 받는다** — `from[]`이 데이터 모델에 실재해야 한다. 비면 "파생이다"라는 말만
  남고 무엇에서 왔는지가 사라져 빈 껍데기가 된다.
- **연산 종류는 enum으로 고정하지 않았다.** 실사용 표본이 아직 얇다(보고된 일곱이 48건에서 나온 수).
- **기계가 못 잡는 반대편을 명시한다** — `dataModel`이 붙었는데 **사실은 파생**인 것(실측 673건 중 25건).
  이름이 실재하니 통과한다. 풀 리뷰의 **미러 충실도**에서 보도록 스킬·안내서에 적었다.

### Added — 받는 쪽 일회성 값 (`transientReceives`)

보내는 쪽에만 `transientSends`가 있고 받는 쪽엔 대응물이 없었다. 그래서 총 건수·다음 페이지 커서 같은
결과값이 갈 곳이 없었고, `receives` 설명이 **"결과값은 변수가 아니면 제외(산문에만 둠)"**라고 적어
**버려지는 자리를 규약이 명시**하고 있었다. 산문에만 있으면 요청서에 안 나가고 백엔드는 그 요구를 모른다.

- 화면 `io[]` · 프레임 `io[]` · 요청서 `requests[]` 셋 다에 추가하고, 생성기가 실어 나른다.
  사람이 읽는 요청서 표에도 **"일회성 결과"** 열이 생겼다.
- 실재 변수를 여기 적으면 **오류**다(`receives`로 옮긴다) — 보내는 쪽과 같은 규칙.
- 내비게이션은 여전히 산문 몫이다(데이터가 아니라 화면 흐름).

### 업그레이드 경로

- **감지** — suite Step 1 공통 gap-fill에 세 신호(항목 모양 없음 · 근거 갈래 없음 · 결과값이 산문에만).
- **채움** — backend·screen-design·data-model 버전업 절. **모르는 것은 지어내지 않고 비워 집계에 남긴다.**
- **가시화** — 점검기가 셋 다 집계(갈래·건수·고칠 자리).
- **검증** — 실사용 세트에서 목록 71건(펼침 38 + 없음 33) · 근거 미기재 276건을 집계.
  보고된 317건에서 컨테이너 45건을 뺀 수와 맞는다.

### 회귀

152건(+12). 옛 코드에 새 회귀를 돌려 **8건 실패** 확인.

---

## [0.11.2] — 2026-08-28 (beta)

> 상태: `beta` 머지로 **버전 0.11.2 확정**, 릴리즈 날짜 = beta 머지일(2026-08-28).
> 근거: GitHub 이슈 #3(바른보고서 SaaS 팀 실사용 관찰) **1차분**. 새 검사는 전부 **경고**라 깨지는 것이 없다.

### 고침 — 근거(`basis`)가 요청서를 안 탄 인터페이스를 못 잡았다

`kind: screen-io` 근거는 **그 동작이 화면 설계서에 실재하나**만 봤다. 그래서 요청이 요청서 밖에서
왔을 때(이슈·운영 요구) `basis`가 필수라 **엉뚱한 화면 동작 id를 갖다 붙인 것**이 그대로 통과했다.
실측 130건 중 4건이 그랬고 **넷 다 진짜 결함**이었다(오탐 0).

- 요청서에 실리지 않은 동작을 근거로 들면 **경고**한다.
- **고칠 자리가 셋으로 갈리므로 갈래를 판정해 안내한다** — 한 갈래로 뭉쳐 "`ops`를 쓰라"고만 하면
  **엉뚱한 곳을 고치게 된다**(실측 4건의 답은 전부 화면 설계서였다: 서버를 거치는 동작이 `target: client`로
  적혀 요청서에서 빠졌고, 그래서 담기·빼기가 저장되지 않았다):

  | 상황 | 갈래 | 고칠 자리 |
  |---|---|---|
  | 화면에 있는데 요청서에 없음 — `target`이 `client`/`local` | `screen-io` 유지 | **화면 설계서** |
  | 화면에 있고 `target: server`인데 요청서에 없음 | `screen-io` 유지 | **요청서 재생성** |
  | 화면 자체가 없음(배치·웹훅·정산·이슈로 온 서버 판단) | **`ops`** + `why` | — |

- **대조 게이트는 "그 화면 설계서가 요청서로 수확된 적 있나"다.** "요청서가 하나라도 있으면 판정"으로
  잡으면 **한 트랙 요청서만 가진 리포**(사용자 앱 요청서는 있는데 백오피스 것은 없는 사본)에서 그 트랙이
  통째로 위반으로 뜬다. 수확된 적 없는 화면의 동작은 **아직 요청서가 없는 것**이지 근거가 틀린 것이 아니다.
- **`ops` 갈래는 원래 있었는데(0.8.0) 안내가 산문 표 한 줄뿐이라 못 찾았다.** 스키마 `basis` 설명과
  백엔드 스킬에 **갈래 고르는 법**을 표로 세웠다.

### 고침 — 같은 뜻을 다른 이름으로 적어도 조용히 통과했다

허용값을 `values`/`enum`, 필수 여부를 `required`/`optional` 둘 다로 적고 있었다(실측: `values` 50 · `enum` 26 ·
`optional` 2). 스키마가 한쪽만 선언해 두어 **나머지는 모르는 키로 통과**했고, **소비자가 표준 이름만 읽으면
별칭으로 적힌 것은 그 값이 없는 필드로 보인다.**

- **표준 이름은 `values`·`required`** — 스키마 설명에 못박고, 점검기가 별칭을 **"이관 필요"로 집계**한다.
- **`response.fields[]`에 `values`·`required` 칸을 열었다.** 없어서 표준 이름 자체가 성립하지 않았다
  (별칭 28건 중 26건이 응답 쪽이었다).
- **`additionalProperties: false`로 즉시 막지 않는다** — 990필드 문서가 그날로 실패한다. 경고로 시작한다.

### 업그레이드 경로

- **감지** — suite Step 1 재진입의 세트 공통 gap-fill에 두 신호 추가(별칭 사용 · 근거가 요청서를 안 탐).
- **채움** — `easyproduct-backend` 버전업 절에 이름 이관과 근거 갈래 판정 절차.
- **가시화** — 점검기가 둘 다 집계해 보고(갈래별 건수 + 고칠 자리).
- **검증** — 실사용 문서 세트 **넷**에 돌려 보고된 4건·28건을 재현하고, 독립 파싱으로 수치를 대조했다.
  게이트를 옛 방식으로 되돌리면 오탐 회귀가 실제로 깨지는 것까지 확인했다.

### 안내서

- `docs/03` — 근거 갈래 고르는 법 표.
- `docs/05` — "③-4 백엔드 계약의 근거·필드 이름" 절 신설(경고를 받았을 때 어디를 고치나).

---

## [0.11.1] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.11.1 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).
> 근거: GitHub 이슈 #2(칼퇴근 프로젝트 요청). **선택 기능이라 깨지는 것이 없다.**

### Added — IA를 범위별 파일로 나눌 수 있다 (선택)

트랙(브랜치)이 갈린 모노레포에서 사용자 앱과 백오피스를 다른 팀이 개발하면 **IA만 소유자가 둘**이라
문서를 합칠 때마다 충돌한다. 실측에서 충돌 4건 중 하나가 IA였고, **내용이 아니라 frontmatter
`revision` 한 줄** 때문이었다(본문 1000여 줄은 3-way 머지로 깨끗이 합쳐졌다).

```
ssot/ia.md              허브 — 요약·링크만. 기계 블록 없음 · machine.includes 로 부분 파일 선언
ssot/ia-user.md         사용자 앱 IA   — 자기 revision + ia.features
ssot/ia-backoffice.md   백오피스 IA    — 자기 revision + ia.features
```

- **새 규약을 만들지 않았다.** 문서를 여러 파일로 나누는 장치는 이미 있다 — **`machine.includes`**.
  데이터 모델·백엔드 인터페이스가 쓰는 것과 같고, 점검기가 그 선언을 따라간다.
  "부분 파일은 조각이 아니라 온전한 문서다"라는 기존 규약이 곧 **리비전 독립**을 뜻한다.
- **범위가 하나면 나누지 않는다.** 한 팀이 다 만드는 제품은 한 파일이 편하다 — **제안이지 강제가 아니다.**
- **허브에는 링크·요약만.** 화면 지도·네비게이션을 남기면 허브가 다시 공동 편집 파일이 되어 충돌이 돌아온다.
- **점검기는 손대지 않아도 됐다** — 등기부 적재가 문서마다 누적이라 `FEAT.*` 크로스참조가 그대로 돈다
  (이슈의 분석이 맞았다). 도그푸드로 나누기 전후 등기부가 같은 것을 확인했다.

### Added — 파일이 갈리면서 생기는 두 가지를 새로 본다

**한 파일일 때는 불가능했던 사고**라 분리보다 **먼저** 넣었다.

- **중복 기능 정의(오류)** — 같은 `FEAT.*`가 두 파일에 있으면 나중 것이 조용히 덮어써 라벨·상태가
  뒤바뀐다. 데이터 그룹에 이미 있던 가드를 기능에도 둔다.
- **범위와 어긋난 기능(경고)** — 나눈 파일에 다른 범위가 섞이면 **소유자를 1:1로 만들려던 뜻이 깨진다**
  (두 팀이 그 파일을 다시 함께 고친다). **판정 근거는 파일명이 아니라 `audience`**다 — 그 값이 정본이고,
  나눌 때도 그걸로 가른다. 범위를 안 밝힌 파일명(단일 IA)은 대상이 아니다.

### Added — 기존 문서 업그레이드 경로

| | 무엇 |
|---|---|
| **감지** | suite Step 1 재진입 — `audience`에 `user`·`admin`이 함께 있는데 한 파일이면 **제안** |
| **채움** | `easyproduct-ia-designer`의 **"범위가 둘인데 한 파일이면 — 나누기"** — `audience`로 가르고, **`FEAT` id는 한 글자도 안 바꾼다** |
| **가시화** | 위 두 검사(중복·범위 어긋남) |
| **검증** | 도그푸드 — 한 파일 → 허브+범위별로 나눠 **등기부가 동일**(`FEAT 2`)한 것 확인 |

> **git이 1→2 분리를 추적하지 못한다.** 여러 브랜치에서 각자 IA를 고치고 있다면 **먼저 합친 뒤에 나눈다** —
> 반대로 하면 다른 브랜치의 수정분이 유실된다(이슈가 지적한 그대로이고, 그래서 급하지 않다).

### Changed — 회귀 129건 (8건 추가)

정상 분리 무경고 · 등기부 합류 · 중복 기능 정의 · 범위 어긋남 · **나누지 않은 세트는 대상 아님**.

---

## [0.11.0] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.11.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).
> **깨는 변경이다** — 옛 세트는 `auth`를 채우기 전까지 점검을 통과하지 못한다.

### Added — 화면 동작의 인증 요건 (`io[].auth`)

요구에 **"누가 쓸 수 있는가"가 없었다.** 인증은 계약 쪽(`backend-interface`의 `auth.mode`)에만 있었고
필수였는데, **그 결정의 근거가 되는 요구는 아무 데도 안 실렸다.** 백엔드가 추측할 수밖에 없었다.

실측이 심각했다 — 서버 동작 **135건 중 82건**이 권한 정보 없이 요청서로 나갔고,
**그중 27건이 운영자 전용**이었다(기관 청구 목록·청구 상세·상품 노출 토글·상품 저장 등).

```json
{ "id": "IO.admin.product.list.toggleVisible", "target": "server",
  "auth": { "required": true, "roles": ["operator"] },
  "policies": ["POL.product.visibility"] }
```

- **`target`이 `server`면 필수**다. 스키마에 `if/then`이 없어 조건부 required를 못 걸므로 **점검기가 오류로
  강제**한다 — 화면 이동·정렬 토글까지 요구하면 소음이라 범위를 server로 한정했다.
- **`required` · `roles` · `note`만 적는다. 방식(`mode`)은 안 적는다** — 토큰이냐 세션이냐는 백엔드가
  정할 계약이다. 요구와 계약의 층을 그대로 유지한다.
- **없는 것을 "인증 불필요"로 간주하지 않는다.** 그러면 안 쓴 것과 "불필요라고 쓴 것"이 구분되지 않아
  **운영자 전용 27건이 공개 엔드포인트가 된다.** `target` 미분류·`basis`의 `why`를 다룬 방식과 같다 —
  **지어내지 않는다.**
- 프레임 `io`·요청서에도 함께 실린다. 요청서 표는 미분류를 빈칸이 아니라 **"미분류"**로 적는다.

### Fixed — 묶기 판단이 드디어 권한을 본다

"권한이 다르면 묶지 마라"가 백엔드 스킬의 **묶기 금지 신호 1번**인데, **그 재료가 요구에 없어 기계는
볼 수 없었다.** 판단이 전부 사람에게 넘어가 있었고, 사람이 놓치면 넓은 쪽으로 열렸다.

- **권한이 다르면 후보로 올리지 않는다**(같은 형태여도).
- **미분류는 자기들끼리도 안 묶는다** — 모르는 것을 아는 척하지 않는다.

### Added — 기존 문서 업그레이드 경로

| | 무엇 |
|---|---|
| **감지** | `❌ 인증 요건(auth)이 없는 서버 동작 n건` + suite Step 1 재진입 트리거 |
| **채움** | `easyproduct-screen-design`의 **"인증 요건 채우기"** — **기계 초안 규칙 5개**(admin·프레임 가드·공개 화면·정책 근거·근거 없음) |
| **가시화** | 같은 집계(어느 동작인지까지) |
| **검증** | 실사용 세트 사전 측정 — 135건 전부 걸리고, **초안 규칙이 약 67%를 덮는다**(운영자 38 · 공개 16 · 정책 근거 37 · 근거 없음 44) |

**이건 깨는 변경이다.** 옛 세트는 업그레이드 전까지 점검을 통과하지 못한다. 그 대가로
**"권한이 안 적힌 요구가 백엔드로 나가는 일"이 구조적으로 불가능해진다.**

### Changed — 회귀 121건 (9건 추가)

`auth` 강제 · client 동작 면제 · **권한이 다르면 안 묶기** · 같으면 묶기 · 요청서 전달(표+블록).

---

## [0.10.5] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.5 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).
> 실사용 제보. **두 스킬이 `version`을 반대로 정의하고 있었다.**

### Fixed — 하네스가 `revision`을 모른 채 "`version`을 올려라"라고 지시했다

`easyproduct-suite`는 **`version` = payload 계약(스키마) 버전이라 내용이 바뀌어도 안 올라간다**고
못박았는데, `easyproduct-sdd-harness`는 **`version`을 문서 수정 카운터로 취급**해
**"상위 문서의 `version`을 먼저 올려라"**라고 5곳에서 지시했다(검사기 2종·정본 수도코드·템플릿 2종).
하네스 전체에서 **`revision`이라는 단어가 0회** 등장했다 — 그 필드가 생기기 전 모델 위에 서 있었다.

**이 하네스를 깐 모든 프로젝트가 같은 오지시를 받았다.** 실측 피해가 양방향이었다.

- 시키는 대로 `version`을 올려 **v1 스키마 문서에 `version: 13`이 박혔다**(스키마 계약 버전 오염).
- 반대로 계약대로 `revision`만 올린 변경을 하네스가 계속 위반으로 보고해,
  **"레포 전역의 규약 위반"이라는 오진**까지 나왔다. 경고 60건 중 실제 요구 변화는 **0건**.

**개정 축을 어댑터에서 받는다.** `easyproduct`면 `revision`, `generic`이면 문서 자신의 버전 표식
(헌법의 `**Version**: 1.0.0` 등 — 그쪽은 올리는 게 맞다). 축이 갈리니 신호도 갈린다:

```
✗ ia.md 개정됨(revision 1→2)                          ← 진짜 할 일
⚠ ia.md가 핀 이후 수정됨(revision 1 그대로 · 해시 불일치)  ← 문구 손질이거나 번호를 안 올린 것
```

- 핀 파일에 `revision`을 적는다. **옛 핀(`version`만)도 그대로 읽는다**(축이 `revision`인데 없으면 폴백).
- FIX 문구도 축을 따른다 — easyproduct 문서에 "version을 올려라"라고 하지 않는다.
- 정본(`checker-pseudocode.md`)부터 고쳐 두 구현·템플릿 2종이 따라오게 했다.

### Added — 우리 쪽 침묵도 함께 막는다

계약서가 "`version`은 내용이 바뀌어도 안 올라간다"고 못박아 놓고 **그게 지켜지는지 보는 층이 없었다.**
`check-docs`가 frontmatter `version`을 읽는 코드조차 없어, `version: 13`이 박혀도 통과했다.

- `machine.schema` 파일명의 `.v<N>`과 frontmatter `version`을 **대조**한다.
- **"이행 필요"로 집계하고 순서를 안내한다** — 그냥 `1`로 되돌리면 슬라이스 핀이 한꺼번에 깨진다.
  **핀 재생성 → `version` 되돌리기 → 핀 재갱신** 순서다.
- 실측: 한 세트에서 **20건**이 드러났다(지금까지 완전히 침묵).

### Added — `skill-lint`가 두 스킬의 정의 어긋남을 막는다

근본 원인은 **같은 필드를 두 스킬이 각자 정의**한 것이다. 하네스는 easyproduct 없이도 도는 것이
설계라 suite 파일을 참조하지 않는다 — 대신 어제 앵커 등기부에 쓴 방식을 그대로 쓴다:
**하네스가 자기 값을 품고, `skill-lint`가 대조해 어긋나면 베타 머지를 막는다.**

### Changed — 패리티 47건 (6건 추가)

`revision` 상승/문구 손질이 **다른 문구로 갈리는지**, easyproduct 문서에 **`version`을 올리라고 하지
않는지**를 고정했다. 넷은 고치기 전 코드에서 실패하는 것을 확인했고, 하나는 **"픽스처 편집이 실제로
적용됐다"**를 먼저 보는 케이스다(안 그러면 "안 잡힘"과 구분되지 않는다).

---

## [0.10.4] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.4 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).
> 실사용 제보. **검증기가 스키마의 절반을 안 보고 있었다** — 잘못 써도 `위반:0`이 나왔다.

### Fixed — `additionalProperties: false`가 적용되지 않았다

```js
if (schema.additionalProperties && typeof schema.additionalProperties === 'object') {
```

`false`는 falsy라 **분기 전체가 건너뛰어졌다.** 이 코드는 `additionalProperties`가 **스키마 객체**일 때
(= 나머지 키를 그 규격으로 검사)만 다루고 **`false`(= 모르는 키 금지)는 처리 자체가 없었다.**

실측 피해: 프레임의 `appliesTo`를 `{ "exclude": [...] }`로 잘못 썼는데(허용 키는 `except`/`only`)
`위반:0`으로 통과했다. 쓴 쪽은 **로그인 화면이 껍데기에서 빠진 줄 알았고**, 실제로는 안 빠져서
스킬이 프레임 절에서 명시적으로 경고한 순환(**"로그인하려면 세션이 있어야 한다"**)이 문서에 박힐 뻔했다.
**키 한 글자 오타의 결과가 "조용히 아무 일도 안 일어남"**이라 가장 찾기 어려운 부류다.

- 메시지에 **허용 키 후보를 함께** 낸다 — 오타는 후보를 보여 주면 즉시 알아본다.

### Fixed — `$ref` 미지원으로 **프레임 `io`가 통째로 무검사**였다

`screen-design-frame.v1`이 `"items": { "$ref": "#/$defs/io" }`를 쓰는데 검증기에 `$ref` 처리가 없었다.
`type`도 `required`도 `pattern`도 없는 **빈 스키마**로 취급돼 `{"완전히엉터리":1}`이 통과했다.

**`$ref`를 쓰는 스키마는 이것 하나뿐이고, 0.10.0에서 우리가 만든 것이다** — 다른 스키마엔 없던 문법을
쓰면서 검증기가 그걸 아는지 확인하지 않았다. 그래서 **0.10.0 이후 만들어진 모든 프레임 블록은 스키마
검증을 한 번도 받지 않았다**(`action`·`sends`·`receives` 필수도, `id`·`target` 패턴도).

- 풀 수 없는 `$ref`는 **오류로 보고**한다 — 조용히 건너뛰면 같은 침묵이 된다.

### Fixed — 생성기가 내는 키를 스키마가 몰랐다 (위 수정이 드러냄)

0.10.0에서 요청서에 `frame`·`appliesTo`를 실으면서 **`interface-request.v1`에 그 키를 안 넣었다.**
`requests[]`는 `additionalProperties: false`라, **우리가 생성한 파일이 우리 스키마를 위반한 채** 나가고
있었다. `additionalProperties`가 안 돌아서 조용히 통과했을 뿐이다. 스키마에 두 키를 추가한다.

### Changed — 사본 일치 검사를 건너뛸 때 **건너뛴다고 말한다**

스킬 자산을 못 찾으면(검사기를 스킬 밖에 두고 실행) 이 검사는 원래 생략하게 돼 있는데 **아무 말도
안 했다.** 지금 출력만 보면 "검사했는데 같다"와 "아예 안 봤다"가 구분되지 않는다.
이 저장소 원칙이 **"무엇을 안 봤는지는 밝힌다"**이므로 생략한 스키마 목록을 보고한다.

### 사전 측정 — 이행 경로는 필요 없다

실사용 세트(문서 82개)에 고친 검사기를 돌렸다.

| | 결과 |
|---|---|
| 사람이 쓴 문서(화면·프레임·데이터·정책…) | **위반 0건** |
| 프레임 블록이 처음으로 검증받음 | **통과** |
| 위반이 난 문서 | **2건 — 둘 다 우리 생성기가 만든 요청서** |

그 2건도 **스키마에 `frame`·`appliesTo`를 넣으면 사라진다.** 사용자가 고칠 문서가 없다
(스키마 사본 갱신은 업그레이드하면 어차피 따라온다). 그래서 감지·채움 경로를 내지 않는다.

### Changed — 회귀 112건 (7건 추가)

**여섯 케이스 전부 고치기 전 코드에서 실패하는 것을 확인**하고 넣었다. 그중 하나는
**"픽스처 편집이 실제로 적용됐다"**를 먼저 보는 케이스다 — 결과가 "아무 일도 안 일어남"인 시험은
**시험 자체가 아무 일도 안 한 경우와 구분되지 않는다.** 이번 제보에서 실제로 그 혼동이 있었고
(제보자가 오보를 스스로 찾아 철회했다), 같은 함정이 우리 회귀에도 그대로 있었다.

---

## [0.10.3] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.3 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).

### Added — 0.10.2가 빠뜨린 **기존 문서 업그레이드 경로**

0.10.2에서 프레임 동작 id 규칙을 바꿔 놓고 **경로를 안 냈다.** CHANGELOG에 "이미 `IO.frame.*`으로
적은 세트는 고치세요"라고 적은 게 전부였다 — `CLAUDE.md`가 반복 실패로 못박은 바로 그 항목이다.

그 사이 실제로 새고 있던 자리: **범위가 하나뿐인 세트는 충돌도 안 나서 조용히 통과한다.**
중복 검사는 두 범위가 부딪혀야 발화하므로, 사용자 앱만 있는 세트는 잘못된 id를 그대로 유지하다가
나중에 백오피스를 추가하는 순간 부딪힌다.

| | 무엇 |
|---|---|
| **감지** | 프레임 동작 id의 **앞 두 마디를 소유 프레임과 직접 대조**한다(중복 여부와 무관) + suite Step 1 재진입 트리거 |
| **채움** | `easyproduct-screen-design`의 **"프레임 동작 id에 범위 넣기"** — id 이관 + 백엔드 `basis` 동반 수정 + 요청서 재생성 |
| **가시화** | `⚠ 소유 프레임과 어긋난 동작 id n건 — 이관 필요` (무엇을 무엇으로 바꿀지까지) |
| **검증** | 도그푸드 — **범위가 하나뿐인 세트**에서 충돌 없이도 잡히고, 이관하면 해제되는 것을 확인 |

검사는 `IO.frame.*`이라는 문자열을 찾지 않는다. **소유자와 대조**하므로 어떤 어긋남이든 잡는다.

- 회귀 5건 추가(총 105건). 핵심은 **"범위가 하나면 중복은 안 나는데 그래도 잡는다"**다.

---

## [0.10.2] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.2 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).

### Fixed — 프레임 동작 id가 두 범위에서 충돌했다

0.10.0이 프레임 동작 anchor를 `IO.frame.<프레임이름>.<동작>`으로 예시했다. 그런데 사용자 앱과 백오피스가
**둘 다 껍데기를 `shell`이라 부르므로** 양쪽이 `IO.frame.shell.session`으로 접혀 **중복 동작 id**가 됐다.
요청서·백엔드 `basis`가 조용히 엉뚱한 동작에 붙을 수 있는 상태였다(실사용에서 즉시 발견).

```
❌ 중복 동작 id: IO.frame.shell.session
❌ 중복 동작 id: IO.frame.shell.logout
```

**원인은 이미 있던 규칙을 어긴 것이다.** 동작 anchor는 **"소속을 앞 두 마디가 드러낸다"**가 규칙이고
(`IO.order.create.*` → `FEAT.order.create`), 프레임도 소유자가 `FRAME.<범위>.<이름>`이므로 그대로
따랐어야 했다. `frame`이라는 마디를 새로 만들면서 **범위가 사라진 것**이 결함이다.

- **`IO.<범위>.<프레임이름>.<동작>`** 으로 바로잡는다 — `IO.backoffice.shell.session` · `IO.user.shell.logout`.
- 회귀 2건 추가(총 100건): 범위가 다르면 같은 껍데기 이름이어도 안 겹친다 · **범위를 빼면 실제로 충돌한다.**
- 중복 검사 자체는 0.8.0부터 있었고 정상 작동했다 — 그래서 즉시 잡혔다.

> `IO.frame.*`으로 이미 적은 세트는 **범위 마디를 넣어 고친다.** id가 바뀌므로 백엔드 계약의 `basis`가
> 그 id를 가리키면 함께 고친다(점검기가 죽은 링크로 잡아 준다).

---

## [0.10.1] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.1 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).

### Changed — 인터페이스 계약 부분 파일 폴더: `apis/` → `interfaces/`

**이 계약은 REST 전용이 아니다.** 0.8.0에서 전송 중립을 핵심으로 삼아 `transport`를
`rest`·`grpc`·`graphql`·`ws`·`queue`로 열고 앵커도 `BEOP`(operations)에서 **`BEITF`(interface)**로
바꿨는데, 부분 파일 폴더만 REST 어휘로 남아 있었다. 큐 토픽·WS 채널이 담긴 파일이 `apis/`에 있으면
어휘가 어긋난다. 문서명(`backend-interface.md`)·블록(`backend.interfaces`)·앵커·짝 폴더
(`interface-requests/`)가 모두 같은 어휘라 여기도 맞춘다. 세트의 다른 폴더가 전부 **축약 없는
소문자 단어**(`ssot`·`supporting`·`reference`·`screens`·`schemas`)인 관례와도 맞다.

**이미 다른 이름을 쓰는 프로젝트는 그대로 둔다 — 업그레이드 경로를 내지 않는다.** 이 폴더 이름은
**관례일 뿐 계약이 아니라서**(부분 파일을 찾는 정본은 진입 파일의 `machine.includes`이고 점검기는
그것만 따라간다) `apis/`로 만들어진 세트도 아무 변화 없이 그대로 돈다. 스킬은 발견해도 **바꾸라고
요구하지 않는다.** 코드에는 `apis`라는 문자열이 애초에 한 글자도 없었다.

---

## [0.10.0] — 2026-08-11 (beta)

> 상태: `beta` 머지로 **버전 0.10.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-11).
> 실사용 제보 4건(백오피스 트랙). **요구가 새는 마지막 갈래를 닫고**, 조용히 통과하던 결함 둘을 막는다.

### Added — 공통 프레임 (`screendesign.frame` · `FRAME.*`)

화면들을 감싸는 **껍데기**(백오피스 LNB·상단바 / 사용자 앱 GNB·푸터)에도 서버와 주고받는 일이 있다 —
세션 조회(진입 가드) · 로그아웃 · 워크스페이스 전환 · 전역 검색 · 알림. 그런데 **어느 화면 것도 아니라
담을 자리가 없었다.** index 문서는 "링크 허브라 기계 블록을 두지 않는다"는 규약이었기 때문이다.

결과는 두 가지로 갈렸다. **백오피스**는 세션 조회·로그아웃이 요청서에 안 나가 옛 손저작 문서를 폐기하지
못하고 **같은 것을 주장하는 문서가 둘 공존**했다. **사용자 앱**은 요청서 96건에 세션 조회가 없고,
로그아웃이 `IO.my.home.logout`으로 **마이페이지에 억지로 붙어** 있었다 — 마이페이지를 안 거치고
로그아웃하는 경로가 문서상 사라졌고, 마이페이지를 개편하면 로그아웃 요구가 함께 흔들린다.

- **`FRAME.<범위>.<이름>`이 새 앵커**다. `FEAT`을 주지 않는 판단은 유지한다 — 프레임은 **목적지가 아니라
  용기**이고 IA의 기능 목록에 없다. 대신 "그러니 기계가 몰라도 된다"를 끝낸다.
- **`appliesTo`로 걸리는 범위를 밝힌다.** 로그인 화면을 안 빼면 **"로그인하려면 세션이 있어야 한다"**는
  순환이 문서에 박힌다. 점검기가 그 화면의 실재까지 대조한다(오타면 가드가 로그인에 남는다).
- **요청서는 `--domain frame`으로 갈린다.** 출처가 index 문서 하나이므로 "출처 문서 1개당 요청서 1개"
  규칙이 유지된다. 요구에 `frame`·`appliesTo`를 실어 **"전 화면에 걸린다"**를 백엔드에 전한다 —
  개별 조회로 오해하면 호출 빈도·캐시·세션 저장소를 잘못 잡는다.
- 프레임도 화면과 **같은 죽은 링크 검사**를 받는다(컴포넌트·데이터·정책·일회성 입력 뒷문).

> 이로써 요구의 갈래가 닫혔다: **화면 안 → 화면의 io · 껍데기 → 프레임의 io · 화면 없음 → 백엔드 basis: ops**.
> 가운데가 비어 있던 동안 그 요구들은 양옆으로 흘러넘쳤다.

### Fixed — 변경 동작의 응답을 조회로 오인했다 (미탐 — 조용히 통과)

0.9.0의 "미조회 데이터" 검사가 `target: server`인 io의 `receives`를 **전부** 조회로 셌다. 그런데
**승인·반려·저장도 결과를 받는다.** 그 응답이 화면 값을 덮으면 잔여가 0이 되어 **진입 조회가 없는데도
통과**했다 — 실측에서 상세 화면 둘이 그렇게 요청서에 조회를 한 줄도 못 내보냈다.

두 번째 신호를 따로 낸다: **"데이터를 보여주는데 진입 로드가 하나도 없는 화면"**.
판정은 0.9.0에서 정한 표식 둘(`id`가 `.load*` · 누르는 것이 아니라 `ui`가 없음)로 한다.

> 제보자는 옛 손저작 요청서와 **건별로 대조**하다 찾았다고 밝혔다 — **대조할 옛 문서가 없는 프로젝트는
> 영영 모른 채 백엔드에 넘긴다.** 그 점이 이 결함의 심각도를 정한다.

### Fixed — 모노레포에서 ③ 결합이 통과 불가능했다

검사기가 바뀐 슬라이스를 `f.split('/')[1]`로 뽑았다. `specsDir`가 **두 마디 이상**이면(모노레포의 정상
설정) `"specs"`가 나와 어떤 슬러그와도 안 맞고, **③ 결합이 항상 발화**한다. `warn`에선 소음이지만
**`block`으로 졸업하는 순간 관장 파일을 건드리는 모든 커밋이 막힌다.**

**그 설정을 우리가 문서로 권했다** — `references/monorepo.md`가 `"specsDir": "frontend-user/specs"`를
권장 예시로 싣고 있다. 프로젝트 설정 실수가 아니라 스킬 결함이다. Node·Python 두 구현을 함께 고치고,
**두 마디 `specsDir` 패리티 케이스**를 넣었다(옛 코드에서 정확히 2건 실패하는 것을 확인).

### Added — `--verbose` · 스키마 오류 힌트

- 목록을 5건에서 자르면 **작업 목록을 만들 수 없다.** 실사용자가 검사기와 같은 판정을 다시 구현했다는
  보고가 있었다. 기본 출력은 짧게 두고(리포트가 읽히려면 짧아야 한다) `--verbose`로 전부 낸다.
- `transientSends`를 문자열 배열로 적으면 "타입 object 아님"만 나와 무엇을 쓸지 알 수 없었다.
  위반 메시지에 **형태 한 줄**(`{ "name": "keyword", "desc": "…" }`)을 곁들인다.

### Added — 기존 문서 업그레이드 경로 (프레임)

| | 무엇 |
|---|---|
| **감지** | "공통 프레임 블록이 없는 화면 설계 index n개" + suite Step 1 재진입 트리거 |
| **채움** | `easyproduct-screen-design`의 **"공통 프레임 블록 채우기"** — 산문에서 껍데기 동작을 찾고, **화면에 잘못 붙은 전역 동작을 회수**하고, `appliesTo` 예외를 적는다 |
| **가시화** | 같은 집계(어느 index인지까지) |
| **검증** | 도그푸드 왕복 — 옛 세트 3경고 → gap-fill → 전부 해제 → 세션 조회·로그아웃이 프레임 요청서로 인계 |

### Changed — 회귀 98건(11건 추가) · 패리티 41건(2건 추가)

그리고 `expect_out` 헬퍼가 `-`로 시작하는 패턴을 grep 옵션으로 먹던 잠복 버그를 고쳤다.

### Fixed — 안내서 두 곳이 0.8.2/0.8.3을 안 따라왔다 (앞서 버전 없이 랜딩한 것)

- `docs/01`의 폴더 구조가 `interface-requests/`를 **평평하게** 그려 두고 있었다. 0.8.2에서 범위별 폴더·
  도메인별 파일로 갈렸고 0.8.3에서 그 옆 `schemas/` 사본이 필수가 됐는데 그림은 옛날 그대로였다.
- `docs/05`의 "다시 뽑기" 절차에 **스키마 사본 옮기기가 빠져 있었다.** 옛 요청서는 세트 루트의 `schemas/`를
  썼는데 새 요청서는 자기 옆을 가리킨다. 안 옮기면 스키마 로드가 실패하고 **그 블록의 검사가 통째로 침묵**한다 —
  시킨 대로 해도 안 되는 절차였다.

둘 다 베타 머지 게이트의 **"기존 안내서의 서술이 사실과 달라졌나"**에 걸리는 건인데 0.8.2·0.8.3 때 놓쳤다.
기능에 붙는 안내서는 챙기면서, **그 기능이 앞선 릴리즈의 서술을 낡게 만드는 것**은 놓친 패턴이다.

---

## [0.9.2] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.9.2 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> **앵커 네임스페이스가 두 곳에서 따로 자라던** 문제. 실사용 제보에서 나왔다.

### Fixed — 새 접두사를 만들고 등기부를 안 늘렸다

0.8.0에서 `IO.*`를 1급 앵커로 만들면서 `machine.namespace`의 값 목록을 안 늘렸다
(`DATA`/`FEAT`/`POL`/`UI`/`SCN`/`token`에 머물러 있었다 — `IO`도 `BEITF`도 없이).

그 결과 SDD 하네스를 쓰는 프로젝트에서 이런 일이 났다: `APIREQ.*` 참조를 새 `IO.*`로 옮겼는데
그 프로젝트의 `sdd-policy.json` `idPrefixes`에 `IO`가 없어, **96건이 죽은 링크로도 안 잡히고
근거로도 안 세어지는 상태**가 됐다. 하네스는 통과했지만 **통과한 게 아니라 안 보인 것**이었다.
그쪽은 자기 배선 실수로 봤지만, **우리 계약 문서를 보고 채웠어도 똑같이 빠졌다.**

- **앵커 접두사 등기부**를 기계 판독 정본으로 냈다 — `easyproduct-suite/assets/anchor-prefixes.json`
  (`FEAT`·`DATA`·`POL`·`UI`·`SCN`·`IO`·`BEITF`·`BESTORE`·`BESCHEMA`·`BEARCH`). checker-guide의 표는 그 사람용 표현이다.
- **skill-lint가 하네스 어댑터 기본값과 대조**한다. 한쪽만 늘면 **베타 머지를 막는다** —
  스킬 간 파일 참조는 최소화하는 원칙이라 하네스가 목록을 품고, 대신 여기서 대조한다.

### Added — `docsAdapter: "easyproduct"`가 실제로 채우는 것

어댑터를 고르는 자리는 있는데 **무엇을 자동으로 채우는지가 정의돼 있지 않았다**(템플릿은 `idPrefixes: []`).
어댑터를 골랐는데 비어 있으면 **⑤ 근거가 아무것도 못 본다.** SKILL.md Step 1에 명시했다 —
접두사 등기부 그대로 + 세트 색인의 `role: ssot`·`handoff` 문서 폴더를 `globs`에 **더한다**(기존 것을 지우지 않는다).

### Added — 정책이 세트를 못 따라간 흔적을 보고한다 (위반 아님)

`idPrefixes`·`globs`는 프로젝트가 손으로 유지하므로 세트가 자라면 반드시 뒤처진다. 그래서 센다.

```
note: ⑤ 등기부에 없는 접두사 참조: IO(96건)
       → 지금은 이 참조들이 죽은 링크로도 안 잡히고 근거로도 안 세어진다(검사 밖).
note: ④⑥ 매니페스트에 있는데 upstreamDocs.globs가 안 덮는 상위 문서 96건 (interface-request 96)
       → 그 문서들은 상위 변경 감지(⑥)·신선도(④) 밖이다.
```

- 앞의 것은 **어댑터와 무관하게** 돈다(순수 spec-kit에서도) — 이번 사고의 직접 원인을 잡는다.
- 뒤의 것은 `easyproduct` 어댑터일 때 세트 색인(`docbundle.docs`)과 대조한다 — 새 채널이 통째로 검사 밖이던 것을 잡는다.
- **정책을 매니페스트로 대체하지 않는다.** 정책은 "무엇을 감시하는지"를 사람이 읽을 수 있어야 하고,
  런타임에 남의 색인에서 끌어오면 그 성질이 사라진다. **채우는 것은 설치·갱신 때, 어긋남은 매번 보고.**
- 오탐 여지가 있어(상수 표기 등) **위반이 아니라 note**다. 리포트·`--json` 양쪽에 실린다.

### Added — 두 점검기의 역할 경계

`ecosystem-overlap.md`의 "남이 이미 하는 검사는 하지 않는다"는 **외부 도구**만 다뤘고,
**같은 세트 안의 `check-docs` ↔ `sdd-check` 경계는 어디에도 없었다.** 그래서 한 프로젝트는
`check-docs`를 CI에 안 물려 두어 새 채널이 **어느 쪽에서도 검증되지 않았다.**

```
check-docs.mjs   세트 문서 내부      스키마 · 죽은 링크 · 파장 · 신선도
sdd-check.mjs    문서 → spec → 코드   핀 · 태그 · 근거 · 역결합
```

조사에 **Step 0-6-1**을 넣었다 — 세트 문서를 발견하면 `check-docs`가 CI에 물려 있는지 확인하고,
안 물려 있으면 함께 걸자고 **제안**한다(대신 깔지는 않는다 — suite 자산이다).

### Changed — 패리티 39건 (4건 추가)

Node·Python 두 구현이 새 note까지 **같은 출력**을 낸다.

---

## [0.9.1] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.9.1 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> **검색인데 보내는 게 없던** 문제 — 저장하지 않는 입력을 적을 자리를 준다.

### Added — `io[].transientSends` (저장하지 않는 일회성 입력)

실사용 세트의 검색 동작이 이랬다:

| 화면 | 동작 | 보냄 | 받음 |
|---|---|---|---|
| `FEAT.lawReview.lawSearch` | 검색 | **—** | `law.id`·`law.name`·`law.summary` |

검색어를 안 보낼 리 없는데 비어 있다. `sends`가 **데이터 모델 실재 변수만** 받는데 검색어는 데이터
모델에 없으니 적을 수가 없었다. 그 결과 백엔드는 **"입력 없는 검색 인터페이스"**를 요구로 받는다 —
조건 없이 전체를 주는 API가 된다.

**데이터 모델에 넣는 것은 답이 아니다.** 데이터 모델은 "서비스가 기억해 둘 정보"이고
**저장 설계의 유일한 입력**이라, 저장하지 않는 값을 넣으면 저장 설계가 **컬럼으로 만든다.**
문제는 제약이 아니라 **적을 자리가 없다는 것**이었다.

```json
{ "id": "IO.lawReview.lawSearch.search", "action": "검색", "target": "server",
  "sends": [],
  "transientSends": [ { "name": "keyword", "desc": "법령명·조문 검색어" } ],
  "receives": ["law.id", "law.name", "law.summary"] }
```

- **`sends` 제약은 그대로 둔다** — 그게 "요구가 데이터 모델과 같은 어휘를 쓴다"를 보장하고 죽은 링크 점검의 근거다.
- **`{name, desc}`로 뜻까지 담는다.** 등기부 대조를 안 받는 자리라 이름만 있으면 나중에 판단할 수 없다
  (컴포넌트 `params`에 "`type`·`desc` 없는 파라미터는 두지 않는다"를 이미 원칙으로 둔 것과 같은 이유).
- **뒷문을 막는다**: 데이터 모델 실재 변수를 여기 적으면 점검기가 오류로 잡고 `sends`로 옮기라고 알린다.
  아니면 이 필드가 `sends` 제약을 우회하는 통로가 된다.
- **`page`·`size`·정렬은 여기 적지 않는다** — 커서냐 오프셋이냐는 **백엔드가 정할 계약**이지 요구가 아니다.
- 요청서(산문 표 + `interface.requests` 블록)에 그대로 실려 백엔드에 닿는다.
  백엔드에서는 `request.fields`가 되되 **저장 대상이 아니다**(`easyproduct-backend`에 명시).
- `receives` 쪽은 **열지 않았다** — 실수요가 나오면 그때 근거를 보고 연다.

### Changed — 회귀 77건 (6건 추가)

대조 면제 · 요청서 산문/블록 반영 · 실재 변수 뒷문 차단 · 이동 안내 · 뜻 없는 이름 스키마 차단.

---

## [0.9.0] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.9.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> **화면이 열리며 실려 와야 하는 것**을 화면 설계서가 말할 수 있게 한다. 실사용 제보에서 나왔다.

### Added — 화면 설계서에 "화면에 들어오면 일어나는 일"

지금까지 화면 산문에는 **"여기서 할 수 있는 일"**밖에 없었고, 그 절은 **누르는 것**만 받았다.
그래서 *화면을 열면 서버에서 실려 와야 하는 것*을 적을 자리가 **문서 어디에도 없었다.**
`display`는 "무엇이 보이는가"만 말하고 **"누가 가져오는가"를 말하지 않는다.**

결과는 조용한 누락이었다 — 실측 세트에서 **화면 52개 중 15개가 인터페이스 요청서에 한 줄도 못 나갔다**
(보여줄 데이터 244개를 선언해 놓고). 백엔드가 그 요청서만 보고 만들면 문의 목록·공지 목록·청구 내역
인터페이스는 **존재하지 않는다.**

- 산문에 **"화면에 들어오면 일어나는 일"** 절을 신설했다. "여기서 할 수 있는 일"은 **사용자가 하는 일만**
  담는다 — 그 절은 "내가 여기서 뭘 할 수 있나"에 답하는 유일한 자리라, 시스템 동작을 섞으면 답을 잃는다.
- `data.io`가 **두 절을 모두 미러**한다. 진입 로드는
  `{id: "IO.<도메인>.<화면>.load", target: "server", sends: [], receives: [보이는 값들]}` · `ui`는 비운다.
- **스키마는 바뀌지 않았다.** `io`는 이미 이걸 담을 수 있었다(`action`은 이름일 뿐이다).
  **수확기(`--emit-needs`·`--emit-interface-request`)도 한 줄 안 바꿨다** — `target: server`면 실으라는
  규칙 그대로다. 고친 것은 **저작 지침**이다: 원본에 없던 것을 원본에 적게 한다.

> 왜 `display`에서 기계로 뽑지 않았나: 그건 **원본(산문)에 없는 것을 파생물에서 지어내는 것**이라
> "산문이 원본, 블록은 미러"라는 축을 뒤집는다. 게다가 기계가 만든 잘못된 분해는 빠진 것보다 위험하다 —
> "이미 있다"는 착시 때문에 아무도 다시 안 본다.

### Added — 상설 검사: 보여주는데 아무도 안 가져오는 데이터

```
⚠ 보여준다고 했는데 아무 동작도 가져오지 않는 데이터 244건 (화면 15개) — 조회 요구가 빠졌을 수 있다
     · FEAT.support.inquiry.list — inquiry.title, inquiry.status, inquiry.createdAt … (5건)
```

`display` 중 **어떤 `server` 동작도 `receives`하지 않는 값**을 화면별로 집계한다.
오탐이 있으므로(정적 문구·앞 화면에서 넘어온 값·화면에서 계산하는 값) **오류가 아니라 정보 층**이고,
판단은 사람·LLM이 한다 — 묶기 후보와 같은 취급이다.

**마이그레이션 도구가 아니라 상설 검사다.** 새 화면을 쓸 때마다 같은 누락이 반복되고,
`display`와 진입 로드를 저작자가 손으로 동기화하는 이상 **그 동기화를 검증하는 유일한 장치**다.

> `display`는 남는다. 진입 로드의 `receives`와 겹쳐 보여도 뜻이 다르고(보인다 / 가져온다),
> **그 차집합이 이 검사의 근거**라 지우면 다음 누락을 못 잡는다.

### Added — 기존 문서 업그레이드 경로

옛 화면 설계서에는 그 절 자체가 없으므로 전부 대상이다.

| | 무엇 |
|---|---|
| **감지** | 위 상설 집계 + suite Step 1 재진입의 낡은 구조 트리거 |
| **채움** | `easyproduct-screen-design`의 **"진입 로드 채우기"** — 값의 출처를 판정해 서버면 `io`에 담고, 정적·전달·계산이면 산문에 한 줄 밝히고, 모르면 `[확인 필요: 이 값의 출처]` (지어내지 않는다) |
| **가시화** | 같은 집계 (화면별로 어느 변수인지까지) |
| **검증** | 도그푸드에서 옛 형식 세트로 왕복 확인 — 요청서 0줄 → 경고 → gap-fill → 경고 해제 → 요구가 실제로 인계됨 |

### Changed — 회귀 71건 (6건 추가)

집계·화면 지목·조치 안내·오탐 안내, 진입 로드가 있으면 안 잡는가(그룹 단위 `receives` 인정),
`local` 동작이 받아도 서버 조회로 치지 않는가.

---

## [0.8.3] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.8.3 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> 0.8.2로 낸 요청서가 **규약대로 두면 점검을 못 받던** 구멍을 막는다. 실사용 제보에서 나왔다.

### Fixed — 요청서가 점검에서 통째로 빠지고 있었다

0.8.2 규약대로 `interface-requests/<범위>/`에 둔 생성물이 자기 스키마를 못 찾으면
`❌ 스키마 로드 실패` 한 줄이 나고 **그 블록은 이후 모든 검사에서 제외된다** — 요구 `ref`·데이터·정책의
죽은 링크도, 낡음 판정도, 옛 배치 감지도 하나도 돌지 않는다. 경고 뒤의 **조용한 통과**다.

- **원인은 경로 계산이 아니라 빠진 지침이었다.** 생성물의 `schema:`는 **자기 옆 `schemas/`**를 가리키는데
  (화면 설계서가 `screens/{scope}/schemas/`를 두는 것과 같은 규칙), 스킬이 그 사본을 복사하라고
  말하지 않았다. 0.8.2에서 파일을 두 단계 아래로 옮기면서 함께 챙기지 못했다.
- 스킬(`easyproduct-screen-design`·suite)과 [03 안내서](./docs/03-프론트엔드-백엔드-연동.md)의 생성 절차에
  **스키마 사본 복사**를 넣었다.
- **점검기가 결과를 명시한다.** 스키마 로드에 실패하면 "이 문서의 기계 블록은 이후 검사에서 전부 제외된다"와
  어디에 사본을 두라는지를 함께 낸다. 오류 한 줄만 내면 사람은 "스키마 하나 못 읽었네"로 읽고
  **그 뒤가 통과한 줄 안다.**
- 생성물에 **`revision: 1`**을 낸다. 생성물은 늘 1세대인데 없어서 점검기가 "업그레이드 필요"로 잡았다.
- 쓰이지 않던 `rel` 헬퍼를 걷었다 — 사본을 문서 옆에 두는 규칙이면 세트 루트까지의 상대 경로가 필요 없고,
  폴더를 옮겨도 그대로 맞는다.

### Added — 왕복 회귀 테스트 (이 구멍이 생긴 이유)

생성 케이스와 감지 케이스를 각각 봤을 뿐, **생성물을 규약 경로에 놓고 다시 점검하는 왕복**을 한 번도
걸지 않아서 못 잡았다. 픽스처를 손으로 써서 상대 경로가 이미 맞았기 때문이다. 이제 건다 —
생성 → 규약 경로 배치 → 재점검(스키마 로드·`revision`·낡음·옛 배치 무경고) → 사본을 치우면
침묵 경고가 나오는지 → 요구가 실제로 화면 동작과 대조되는지. 총 65건.

### Added — 재생성 스윕 규약

도메인이 사라지거나 그 도메인의 서버 동작이 0건이 되면 파일이 안 나온다. **재생성은 범위 폴더의 생성물
전체를 대상으로 하고, 이번 실행에 안 나온 파일은 지우되 지운 목록을 보고한다**(조용히 지우면 유실과
구분되지 않는다). `schemas/`와 생성물이 아닌 파일은 건드리지 않는다. 규약이 없으면 각자 다르게 만든다.

---

## [0.8.2] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.8.2 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> 인터페이스 요청서의 **입자**를 고쳤다 — 범위 통짜 한 파일에서 **범위별 폴더 · 도메인별 파일**로.

### Changed — 요청서는 출처 화면 문서 1개당 1개다

**파생물의 입자는 출처 문서의 입자를 따라야 한다.** 통짜 한 파일은 `from[]`에 그 범위의 화면 문서를
전부 담아서, **화면 하나만 고쳐도 요청서 전체가 낡음**이 됐다. 백엔드는 어디가 낡았는지 알 수 없고
90%가 멀쩡한데 전부를 의심해야 했다. 도메인별로 가르면 출처가 정확히 하나라 판정이 정확해진다.

화면 단위로 더 잘게 가르지 않는 이유: **화면은 문서가 아니라 문서 안의 절**이라 `revision`·`contentHash`가
없다. 화면 단위로 갈라도 같은 도메인의 요청서들이 함께 낡아 파일만 늘고 해상도는 그대로다.

```
screens/user/                        interface-requests/user/
├── screen-design-user-auth.md   →   ├── interface-request-user-auth.md
├── screen-design-user-order.md  →   ├── interface-request-user-order.md
└── screen-design-user-help.md   →   (서버 동작 0건이면 파일 없음)
```

- 범위를 **폴더**로 올린 것은 **담당 개발자가 갈리는 경계**이기 때문이다(사용자 앱 / 백오피스 / 백엔드).
- `--scope`·`--domain`이 **필수**가 됐다. 도메인 목록은 `--list-domains`로 얻어 돌면서 뽑는다.
- 블록에 `domain`이 추가됐고 `scope`와 함께 **필수**다(`interface-request.v1` 스키마).

### Fixed — `--scope`를 빼면 권한 경계를 넘는 묶기를 제안했다

범위를 안 주면 필터가 통째로 열려 사용자 앱과 백오피스 요구가 한 목록에 담겼고, 그 목록으로 묶기 후보를
계산해 **권한이 다른 요구를 "하나로 묶을 후보"로 제안**했다. 백엔드 스킬이 묶기 금지 신호 1번으로 둔
바로 그 경우이며, 묶이면 넓은 쪽으로 열려 정보가 샌다. 이제 범위 없이는 뽑히지 않는다.

### Changed — 묶기 후보는 넓게 계산하고 좁게 싣는다

묶기 후보 계산의 존재 이유가 **화면(그리고 대개 도메인)을 가로지르는 중복 찾기**라, 도메인 안만 보면
`IO.auth.*`와 `IO.mypage.*`의 중복이 **경고 없이 조용히** 사라진다. 그래서 계산은 **범위 전체**로 하고,
파일에는 **자기와 얽힌 후보만** 싣는다(도메인 담당자가 남의 목록을 읽지 않도록). 상대가 다른 도메인이면
괄호로 어느 요청서에 있는지 밝힌다.

### Added — 옛 배치 감지와 이행 경로

- 점검기가 **"옛 배치(범위 통짜) 인터페이스 요청서 n건 — 다시 뽑기 필요"**를 집계해 보고한다
  (블록에 `domain`이 없거나 `from` 출처가 여럿인 경우). 화면 문서가 없는 리포에서도 보고한다.
- suite Step 1 재진입의 낡은 구조 트리거, `easyproduct-screen-design`의 gap-fill 절차,
  [05 안내서](./docs/05-기존-세트-업그레이드.md)에 이행 절차를 넣었다 — 요청서는 파생물이라
  **다시 뽑는 것이 곧 업그레이드**다(옛 파일 삭제 + 색인 갱신이 함께 간다).
- 회귀 테스트 12건 추가(총 57건).

---

## [0.8.1] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.8.1 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> 기능이 아니라 **설명**을 채운 릴리즈다 — 0.3.0~0.8.0에 얹힌 것들이 어디에도 설명되지 않은 채였다.

### Added — `docs/` 주제별 안내서 5종

`SKILL.md`는 **에이전트가 실행할 절차**이고 `checker-guide.md`는 **기계 계약의 정본**이라, 둘 다
"이 기능이 왜 있고 언제 쓰나"를 사람에게 설명하지 않는다. 그 빈자리를 채운다. 루트 `README.md`에는
**목차 한 블록만** 두고 본문은 옮기지 않는다.

| 문서 | 읽는 사람 |
|---|---|
| `01-문서-세트-훑어보기` — 어떤 문서가 있고 어떻게 이어지나·SSOT·폴더·ID 체계 | 문서를 만드는 사람 |
| `02-변경-전파와-신선도` — 결정 하나를 바꿨을 때 무엇이 따라오나·파장 지도·`revision`·리뷰 기록 | 문서를 고치는 사람 |
| `03-프론트엔드-백엔드-연동` — 화면 동작이 서버 계약이 되는 4층·요청서·전송 결정·요구 묶기 | 개발자·에이전트 |
| `04-SDD-하네스-연동` — 7규칙 3축·게이트 배선·두 점검기의 분담 | 개발자·에이전트 |
| `05-기존-세트-업그레이드` — 점검기 신호를 읽고 옛 문서를 최신 계약으로 올리기 | 유지보수하는 사람 |

각 문서는 머리에 **읽는 사람**을 밝히고, 정본을 재서술하지 않고 링크하며, 끝에 **"정직한 한계"**를 둔다 —
기계 통과를 검증 완료로 읽히게 두지 않기 위해서다.

### Added — 릴리즈 원칙: `docs/` 현행화가 `beta` 머지의 차단 조건

기능을 여러 번 얹는 동안 안내서가 안 따라오면 **동작은 하는데 아무도 모르는 기능**이 쌓인다. 사용자가
"설명을 제대로 안 해준다"고 말할 때는 이미 여러 버전이 지난 뒤다. 그래서 게이트로 올렸다.

- **판단 기준**(하나라도 예면 차단): 사용자가 **새로 하게 되는 일**이 생겼나 / 기존 서술이 **사실과
  달라졌나**(경로·필드명·절차·출력 문구) / 점검기가 **새 경고를 내는데** 대응 안내가 없나.
- **필수 산출물**: 해당 주제 문서 수정 → 어디에도 안 맞으면 **새 문서를 만들어 README 목차에 링크** →
  정본 재서술 금지(링크) → "정직한 한계" 유지 → 읽는 사람 명시.
- 근거는 `CLAUDE.md`에, 체크 항목은 `VERSIONING.md` 베타 체크리스트에 둔다. 기계로 판정할 수 없어
  (문서가 사실과 맞는지는 읽어야 안다) **의미 판단 항목**이다. 문구·버그 수준 변경은 대상 아님.

### Changed — 인터페이스 요청서의 기본 경로 확정

- 기본 경로는 **`interface-requests/`**(범위별로 갈림: `interface-request-user.md`). 기존 폴더가 전부
  축약 없는 소문자 단어(`ssot`·`supporting`·`reference`·`screens`·`schemas`)라 그 관례를 따랐다.
- 다른 위치를 써도 되지만 **색인에 등재**해야 점검·파장 대상이 된다.

### Fixed — 요청서를 **읽는 쪽**에 경로가 없었다

경로를 만드는 쪽(`easyproduct-screen-design`·suite·checker-guide·스키마)에만 넣고, 정작 그 문서를 읽는
`easyproduct-backend`에는 안 넣었다. "있으면 먼저 쓴다"고만 하고 어디를 보라는 말이 없으니 실제로는
못 찾고 화면에서 다시 뽑게 된다.

- 입력 표에서 `interface-request`를 **1순위**로 올리고, 화면 `data.io`는 "요청서가 없을 때의 입력"으로 내렸다.
- 찾는 법은 **경로로 단정하지 말고 색인의 `docType`으로** — 프로젝트가 다른 위치를 쓸 수 있기 때문이다.

---

## [0.8.0] — 2026-08-10 (beta)

> 상태: `beta` 머지로 **버전 0.8.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> 백엔드 설계 스킬 편입 — 그리고 그에 맞춰 **요구(무엇이 필요한가)와 계약(어떻게 주고받나)의 층을 갈랐다.**

### Added — `easyproduct-backend` (신규 스킬)

기획 문서 세트를 근거로 백엔드를 설계한다. 산출물은 **아키텍처 · 저장 설계 · 논리 스키마 · 인터페이스 계약**
네 문서이며 뒤 셋은 그 영역의 SSOT다. 파이프라인 **뒷부분**(화면 설계 뒤, spec-kit 인계 전)에 온다 —
데이터 모델과 화면의 동작이 있어야 성립하기 때문이다.

- **전송 중립**: 계약에 REST를 박지 않는다. `transport`(rest·grpc·graphql·ws·queue·other)를 먼저 정하고
  그 전송에 걸맞은 규격을 `binding`에 담는다(REST면 method·path·status, gRPC면 service·rpc, 큐면 topic…).
  앵커는 `BEITF.*` — 문서가 '인터페이스 계약'이니 항목도 인터페이스다(큐 토픽·WS 채널을 '오퍼레이션'이라
  부르는 것도 어색하다).
- **요구의 출처를 갈래로 연다**: `basis[]`(필수·다중) = `screen-io` · `api-request` · `ops` · `policy` · `legacy`.
  **특정 요청서 형식을 전제하지 않는다.** 등기부 없는 갈래(`ops`·`legacy`)는 `why` 필수 — 그래서
  **화면이 없는 API가 예외가 아니라 근거를 갖는 정식 경로**가 된다.
- 언어·프레임워크를 특정하지 않고 저장소도 **종류**까지만 정한다. 데이터의 뜻(데이터 모델)·제품 규칙(정책서)을
  침범하지 않고, 근거가 없으면 `[확인 필요]`로 남긴다.

### Added — 인터페이스 요청서 (`interface-request`)

**백엔드가 프론트의 화면 설계서를 뒤지는 것은 층 침범**이다. 프론트가 "우리가 필요한 건 이것"을 파일로
넘기고, 백엔드는 그 파일만 읽는다.

- **기계가 생성한다**: `check-docs.mjs --emit-interface-request [--transport rest|grpc|…] [--scope user]`.
  화면 동작(`target`이 서버인 것)에서 뽑아 md 문서로 출력한다 — 사람이 옮겨 적지 않는다.
- **전송은 `preferredTransport`(희망)이고 확정이 아니다.** 그 전송이면 백엔드가 채워야 할 자리(`bindingSlots`)를
  함께 싣되 **경로·메서드 값은 지어내지 않는다**(그건 요구가 아니라 계약이다).
- **SSOT가 아니라 파생물**이다(`role: handoff`). 손으로 고치지 않고 다시 생성하며, 화면과 어긋나면 화면이 이긴다.
  `from[]`의 **출처 스냅샷**(화면 문서의 revision+해시) 덕분에 화면이 개정되면 점검기가 "요청서가 낡았다"를 잡는다 —
  파일로 굳히는 대가를 관리하는 장치다.
- 요청서의 `ref`(`IO.*`)·데이터 변수·`policies`는 전부 죽은 링크 점검을 받는다.

### Changed — 화면 설계 계약 (`screen-design.v1`의 `io[]`)
- **`target` 추가** (`server`|`local`|`client` + 한정자 `server.MAIN_SERVER`) — **io는 서버 통신 전용이 아니다.**
  로컬 저장·화면 안 상태 변경도 동작이라 여기 담기는데, 구분이 없으면 백엔드가 **로컬 저장까지 서버
  인터페이스로 지어낸다.** 판정은 **첫 마디**로 하므로 나중에 한정자를 붙여도 계약이 깨지지 않는다.
- **`id` 추가**(`IO.<도메인>.<화면>.<동작>`) — 요청서·백엔드 `basis`가 가리킬 안정적 앵커. **`FEAT.`를 쓰지 않는다** — `FEAT.<d>.<n>.<list|detail|…>`가 이미 화면이라 접두사만으로 구분이 안 되면 '접두사가 곧 등기부'라는 대전제가 깨진다.
- **`policies`·`semantics` 추가** — 화면 산문에 있는 규칙 ID와 행동 규약(멱등·오류 구분·정렬)을 동작 단위로 담는다. 안 담으면 **손실 미러**이고, 백엔드가 오류 근거·멱등·정렬을 정할 근거가 통째로 안 넘어간다. `policies`는 `POL.*`라 죽은 링크 점검을 그대로 받는다.
- **`op` 폐기** — 화면↔계약 양방향 기입은 **이중 기입**이고(한쪽이 바뀌면 조용히 어긋난다) 화면이 백엔드를
  알아야 해서 독립성도 깨진다. 방향은 **백엔드 → 요구** 하나로 통일.

### Added — 계약 등록·점검기
- `checker-guide`: `doc_type` 4종 · 앵커 라우팅 5행 · **"요구와 계약 — 방향은 하나다"** 절.
- 파장 지도에 `backend-*` 4종.
- `check-docs`: 백엔드 등기부 적재 + 저장소·`auth.mode`·`holds.group` 참조 검사 + `basis` 갈래별 라우팅 +
  **양방향 커버리지**(server 요구 미덮임 / 근거 0 금지) + `target` 미분류·폐기된 `op`·한정자 표기 집계.
- suite: **Stage 3.9(백엔드 설계, 옵트인)** · **백엔드 spec-kit 인계장**(`speckit-handoff-backend-template.md`) ·
  Step 1 재진입 트리거에 `target` 없음·`op` 잔존 추가.
- 회귀 6건 추가(점검기 테스트 22케이스).

---

## [0.7.0] — 2026-08-09 (beta)

> 상태: `beta` 머지로 **버전 0.7.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-10).
> 변경 전파(change propagation) 층 신설 — 상위 결정이 바뀌었을 때 하류를 구조에서 예측해 재도출한다.

### Added — 변경 전파(change propagation)

확정된 상위 결정이 바뀌었을 때 하류 문서를 **구조에서 예측해** 다시 도출하는 경로가 없었다.
그래서 실행자는 검색(grep)으로 대신했고, 산문에만 있는 파장(참조 ID가 없는 것)은 통째로 샜다
— 화면 정의의 옛 표현·컴포넌트 파라미터 누락·약관 누락이 한 번에 드러난 실제 사고가 계기다.

- **파장 지도**(`easyproduct-suite/assets/propagation-map.json` + `references/propagation-map.md`) —
  문서 **종류** 사이의 파생 관계를 스킬 자산에 정본으로 둔다. 프로젝트 매니페스트에 손으로 적게 하지
  않는다(손으로 유지되는 중복은 틀어지면 **거짓말하는 지도**가 되고 authoring을 무겁게 한다).
  표준과 다른 상류를 갖는 문서만 매니페스트의 **`derivesFrom`(선택)**으로 덮어쓴다.
- **`revision`(결정 개정 번호)** frontmatter 필드 — 그 문서의 **결정이 바뀔 때만** +1(오타·문구는 올리지 않는다).
  `version`(payload 계약 버전)과 다른 축이며, 파장·신선도 판정의 1차 신호다. 7개 저작 스킬에 규율을 넣었다.
- **리뷰 산출물**(`doc_type: review` · `review.v1` · `assets/review-template.md`) — 점검 3층 중 **LLM 층에만
  산출물이 없어** 건너뛰어도 표가 나지 않던 비대칭을 없앤다. 무엇을 확인했는지(상류→하류 쌍)와
  그때 상류의 `revision`·`contentHash` 스냅샷을 남긴다. 없으면 점검기가 **"LLM 층 미실행"**을 보고한다.
- **변경 전파 모드**(suite `Step 1-B`) — 재진입의 **세 번째 갈래**. 진원지 특정 → 파장 지도로 하류 계산 →
  파이프라인 순서로 두 질문("이 문서에 무엇을 낳는가" / "여기 새로 생긴 요구가 다음 문서에 무엇을 낳는가")
  → `revision` 갱신 → 리뷰 산출물 → 3층 점검. **범위 증가는 실패 신호**(지도 밖 문서 2건 초과 시 지도 재계산,
  기록은 `outOfMap`에 남겨 다음에 지도를 고치는 근거로 쓴다). **완료 어휘 규칙**도 전 Stage 공통으로 승격.

### Added — 점검기(`check-docs.mjs`) 확장
- **[3] 파장·신선도 검사**: 파장 지도로 상류→하류 쌍을 계산하고, 최신 리뷰 스냅샷과 현재 문서를 비교해
  ⓐ `상류가 개정됨(rN→rM) — 하류 재검토 필요` ⓑ `개정 번호 없이 수정됨` 두 가지로 구분해 경고한다(차단 아님).
- `--print-snapshot`: 리뷰 산출물에 붙일 스냅샷을 출력(해시를 손으로 만들지 않게).
- **버그 수정**: 기계 블록이 없는 문서(기획서·약관·화면 색인)가 내부 목록에서 빠져 파장 대상에서 통째로
  누락됐다 — 실제 사고 3건 중 2건이 바로 그런 문서였다.
- `scripts/check-docs-test.sh` — 그 사고를 소재로 한 회귀 검사 10케이스.

## [0.6.0] — 2026-08-03 (beta)

> 상태: `beta` 머지로 **버전 0.6.0 확정**, 릴리즈 날짜 = beta 머지일(2026-08-03).
> 새 스킬 `easyproduct-sdd-harness` 추가 — 문서를 만드는 세트와 **별개 층**인 개발자용 하네스 설치 도구.

### Added — `easyproduct-sdd-harness` (신규 스킬)

spec-kit(SDD) 프로젝트에서 "모든 개발이 실제로 spec-kit을 거치도록" 기계적으로 강제하는 게이트를
**설치·배선**하는 개발자용 스킬(installer/wirer). 제품 SSOT(기획서·spec·plan)는 저작하지 않고,
그것이 지켜지는지만 본다. 발동 조건은 대상 프로젝트의 `.specify/` 존재이며, easyproduct 문서가
하나도 없는 순수 spec-kit 프로젝트에서도 완결 동작한다.

- **막으려는 실전 실패 3종** — C1 SDD 절차 없이 소스부터 고침 · C2 상위 문서(기획서·SSOT·헌법)가
  바뀌었는데 spec-kit 흐름을 못 탐 · C3 상위와 충돌하는 spec 수정.
- **7규칙 3축** — 코드 층(①출처 태그·②완결·③결합) / 상위→spec 전파 층(⑥역결합·④신선도, `version`+`contentHash`
  **이중 핀**) / spec 내용 층(⑤근거 앵커·⑦리뷰 기록). **C2 축은 spec-kit 본체·커뮤니티 확장 어디에도
  없는 빈칸**이라 이 스킬의 핵심 가치다.
- **문서 어댑터 2종** — `easyproduct`(기계 블록에서 앵커 등기부를 만들어 정밀 대조) / `generic`(헤딩·`REQ-*`).
  어댑터는 정책 문자열만 받으며 `check-docs.mjs`·세트 스키마를 import하지 않는다(독립성 불변식).
- **참조 구현 2종** — 무의존 `scripts/sdd-check.mjs`(Node)·`scripts/sdd_check.py`(Python). 같은 정책·같은
  저장소에서 **동일한 위반 목록·종료코드**를 내며, `scripts/parity-test.sh`가 더미 프로젝트로 그 동등성과
  회귀 35케이스를 검사한다.
- **중복 회피** — 이미 깔린 spec-kit 확장을 조사해 겹치는 검사를 위임한다(②완결 → CI Guard, ⑦의 리뷰 실행
  → `/speckit.analyze`, 배선 → Gates의 3경계). 어느 확장에도 의존하지 않고 전부 없어도 완결 동작한다.
- **정직한 한계 명시** — 7규칙은 의례·공존을 강제할 뿐 인과적 파생·의미 정합을 검증하지 않는다
  (기계 통과 ≠ 검증 완료). 리포트는 끄거나 위임한 규칙을 머리말에 드러낸다.

### Changed
- **스킬-린트에 installer 스킬 예외 추가**(`skills/easyproduct-suite/scripts/skill-lint.mjs`).
  문서를 산출하지 않는 스킬은 문서 점검 3층 계약의 대상이 아니라, "점검 3층·정합성 점검" 대신
  **"설치한 게이트가 무엇을 보장하지 않는지(한계)"를 SKILL.md에 명시**하도록 요구한다
  (게이트를 약화시키지 않는 대체 요구). 근거는 `references/checker-guide.md`.

## [0.5.0] — 2026-07-23 (beta)

> 상태: `beta` 머지로 **버전 0.5.0 확정**, 릴리즈 날짜 = beta 머지일(2026-07-23).
> 데이터 모델 필드 계약 정리(이중 기입 제거 + 뜻이 드러나는 이름) + 스키마 8개 설명 전면 보강.

### Changed (계약 변경 — data-model.v1 payload)
- **`usedIn`("쓰이는 화면") 제거.** 화면↔데이터 관계의 원본(SSOT)은 이미 화면 설계서의
  `screendesign.screens`의 `data`(`display`·`io.sends/receives`·`bindings[].vars`)다. 데이터 모델에
  역방향 인덱스를 손으로 또 적는 것은 **이중 기입**이라, 화면이 바뀔 때마다 데이터가 안 바뀌었는데도
  데이터 모델이 수정되어(가짜 변경 신호) 양쪽이 조용히 어긋났다. 게다가 점검기가 이 필드를 전혀
  보지 않아 틀려도 경보가 없었다. **저장하지 않고 화면 설계서에서 유도**하는 쪽으로 바꿨다.
- **`source` → `filledBy`, 값은 `system` / `user`.** 기존 `source`는 이름이 데이터 계보("어느 시스템에서
  왔나")로 읽혀 "값을 더 넣자"는 오해를 불렀고, 값 자체도 축이 섞여 있었다 — `auto:managed`는 런타임
  속성인데 `auto:preset` vs `user`는 **저작 이력**(스킬이 깔았나 사람이 요청했나)이라 문서 완성 뒤엔
  소비자가 없었다. 이제 **"누가 값을 채우는가"** 한 축만 다룬다.
- 사람용 표 열 이름도 뜻이 드러나게: **`표시` → `누가 넣나`**(값: 시스템 / 사용자).
  "자동으로 깐 항목"은 문서 열이 아니라 **회신 메시지**로 알린다.

### Added (스키마 이행 — 옛 형식 문서를 지금 형식으로) *(한시적)*
> 이 절의 기능은 **0.5.0의 제자리 계약 변경을 흡수하기 위한 한시적 장치**다. 배포 초기라 옛 형식
> 문서가 거의 없으므로, **다음 업그레이드 때 삭제 여부를 컨펌받고 뺀다.**

- **데이터 모델 스킬에 [⑤ 스키마 이행]을 추가**했다. 기계 층은 이미 있는데 **필드 구성이 옛 계약**인
  문서를 내용 보존한 채 지금 계약으로 바꾼다. **[② 수정]의 "버전업"(기계 층이 아예 없는 문서에
  채워 넣는 gap-fill)과는 별개**이며, 한 문서에 둘 다 필요할 수 있다.
- 이행 규칙: `source: auto:managed`→`filledBy: system`, `source: user`→`filledBy: user`(기계적),
  `usedIn`→삭제, 표 열 `표시`→`누가 넣나`·`쓰이는 화면`→삭제.
- **`source: auto:preset`은 기계적으로 바뀌지 않는다.** 그 값은 "스킬이 이 필드를 깔았다"는 저작
  이력이라 값을 누가 채우는지와 무관했다. 필드의 **뜻을 보고** 정한다 — 같은 `auto:preset`이라도
  `user.email`은 사람이 적으니 `user`, `user.point`(적립금)는 시스템이 계산하니 `system`이다.
  **판단해서 정한 필드와 버린 `usedIn` 내용은 회신에서 반드시 알린다.**
- 이행은 **표기를 옮길 뿐 뜻을 바꾸지 않는다.** 옛 분류가 틀려 보여도 임의로 고치지 않고 회신에서 짚어 사용자가 정하게 한다.

### Fixed (점검기 — 옛 문서가 조용히 통과하던 문제)
- **스키마 사본 일치성 검사.** 문서 옆 `schemas/*.json`이 스킬 자산과 다르면 보고한다. 옛 문서는
  **자기 옛 스키마 사본을 달고 다니므로 그 낡은 계약으로 검증되어 `위반 0`으로 통과**했다(실험으로 재현).
  **어느 쪽이 뒤처졌는지는 단정하지 않는다** — 문서를 옛 스킬로 만들었을 수도, 반대로 스킬을
  업그레이드하지 않아 스킬 쪽이 옛것일 수도 있다. "다르다"까지만 보고하고 판단은 사람이 한다.
  (스킬 자산을 찾을 수 없는 환경에서는 이 검사를 생략한다.)
- **옛 계약 흔적 탐지** *(한시적 — 0.5.0 이행용)*. `data-model` 문서의 블록에 `source`·`usedIn`이
  남아 있으면 "옛 스키마 형식 — 스키마 이행 필요"로 안내한다. `version` 값으로는 구분되지 않아
  형태로 판별한다. 기존에는 `filledBy 누락`만 나열되어 무엇을 해야 하는지 알 수 없었다.
  **`data-model` 문서에만 적용한다** — `source`·`usedIn`은 흔한 낱말이라 다른 문서 타입이 나중에
  정당하게 쓸 수 있고, 전역으로 잡으면 그때 거짓 경보가 된다.
  **이행이 끝나면 필요 없는 검사이므로, 다음 업그레이드 때 삭제 여부를 컨펌받고 뺀다.**

### Added (목업 파일에 화면 ID 추적)
- **HTML 목업이 어느 화면 설계인지 추적되도록** 화면 ID를 세 곳에 박는다: **파일명**
  `목업-[한글이름]([화면 ID]).html`, 파일 **맨 위 주석**, 최상위 컨테이너 **`data-screen`** 속성.
  목업은 `temp/`라 점검기가 보지 않으므로(재생성·gitignore), 이 셋이 목업↔글 정의를 잇는 추적 고리다.
- 넣는 ID는 **화면종류까지 포함한 화면 ID**(`FEAT.<도메인>.<기능명>.<화면종류>`)이고, 그 화면 글 정의
  제목의 ID를 그대로 옮긴다. 기능 ID만 넣으면 한 기능의 여러 화면(list·detail·edit) 목업 파일명이
  충돌하므로 화면종류까지 넣는다.
- 파일명은 **사용자·백오피스 같은 규칙**이다. 백오피스에 `-관리자` 표기를 붙이지 않는다 — 화면 ID가
  `FEAT.admin.*`이라 관리자 화면임이 ID에 이미 드러나 중복이기 때문이다.

### Added (데이터 모델 파일 분리 — `machine.includes`)
- **데이터 모델을 카테고리별로 나눌 수 있다**(기본은 여전히 한 파일). 데이터 모델은 여러 사람이 고쳐 쓰는
  계약서라, 당사자가 많아지면 한 파일을 두고 부딪히기 때문이다. **경계·파일명은 쓰는 사람이 정한다.**
- 진입 파일 frontmatter에 **`machine.includes: [경로, …]`**로 부분 파일을 선언한다. 이 선언이
  "이 데이터 모델이 무엇으로 이루어졌는가"의 **정본**이라, **부분 파일이 세트 폴더 밖에 있어도** 된다.
  (폴더 스캔·`00-index.md` 매니페스트는 부분 파일의 위치를 보장하지 못하므로 근거가 될 수 없다.)
- 부분 파일은 조각이 아니라 **그 자체로 온전한 문서**다(자기 frontmatter·표·JSON 미러·스키마 사본).
- 불변식: **한 그룹은 한 파일에만**, 그룹 이름은 파일이 달라도 **전역 유일**, `relations`는 파일을 넘나들어도 됨.
  전체 그룹 목록은 저장하지 않고 조회 시 **유도**한다(부분 파일이 바뀔 때 어긋나지 않게).

### Fixed (점검기)
- **중복 그룹 정의를 잡는다.** 같은 그룹이 두 문서에 있으면 처리 순서에 따라 한쪽이 조용히 덮여,
  멀쩡한 필드가 **엉뚱한 문서에서 죽은 링크로** 나타나고 머신마다 결과가 달라졌다(`readdirSync`는
  순서를 보장하지 않는다). 이제 양쪽 파일 경로와 함께 오류로 보고한다.
- **`machine.includes`를 따라간다.** 선언된 부분 파일은 세트 폴더 밖이어도 반드시 점검하고,
  파일이 없으면 어느 문서가 선언했는지와 함께 오류로 보고한다. 같은 파일이 폴더 스캔과 `includes`
  양쪽에서 잡혀도 **실제 경로 기준으로 한 번만** 처리한다(중복 그룹 거짓 경보 방지).
- frontmatter 파서가 **인라인 배열**(`[a, b]`)과 **따옴표**(홑·겹)를 처리한다. 공백이 든 경로는 따옴표
  없이도 되고, 쉼표·`#`가 든 경로는 따옴표로 감싸면 된다(따옴표 안의 `#`는 주석으로 잘리지 않는다).

### Docs (스키마 설명 보강 — 오해 재발 방지)
- **스키마 8개 전부의 `properties`에 상세 `description`을 달았다**(설명 누락 0, 빈약 0).
  이번 오해의 근본 원인이 "설명이 부실해 키를 재해석할 여지가 있었다"는 점이라, 각 키에
  **무엇을 담고·무엇은 담지 않으며·왜 그런지**를 적어 재해석 여지를 닫았다.
- 특히 7개 스키마의 **최상위 내용 배열**(`tokens`·`features`·`rules`·`scenarios`·`screens`·
  `components`·`docs`)에 설명이 아예 없었다 — "빈 껍데기 금지"가 겨냥한 바로 그 필드들이라
  "무엇을 담고 왜 비우면 안 되는지"를 명시했다.

### 검증
- 도그푸드(새 아이디어로 전체 흐름 재주행): 데이터 모델 8그룹·71필드 생성, 표↔JSON coverage 100%,
  세트 점검기 `check-docs.mjs` **위반 0 · 크로스도큐먼트 참조 44건 전부 연결(죽은 링크 0)**.
  화면 설계서의 데이터 축이 `usedIn`이 담던 것을 방향(보여줌/보냄/받음/바인딩)까지 나눠 담아,
  제거로 잃은 정보가 없음을 확인했다.
- `skill-lint.mjs` 통과.

---

## [0.4.4] — 2026-07-20 (beta)

> 상태: `beta` 머지로 **버전 0.4.4 확정**, 릴리즈 날짜 = beta 머지일(2026-07-20).
> 실사용 QA 갭 — "업그레이드(구조) 완료"를 "검증 완료"로 오인하는 혼동 차단.

### Docs (문서)
- **gap-fill(구조 업그레이드) 후 기계층 통과를 "검증 완료"로 보고하는 혼동을 절차로 차단.** 실제 스킬로
  문서를 업그레이드할 때, 층 1~3(기계)만 돌리고 "통과"로 보고해 의미 문제(산문·미러 충실도)가 안 드러난 사례.
  - `checker-guide` "부분 풀 리뷰"에 "**구조 업그레이드는 '수정 후'의 가장 큰 경우 — 기계 통과 ≠ 검증. 업그레이드 뒤
    LLM 풀 리뷰(산문·미러 충실도)를 이어붙이거나 '의미 점검 아직'을 명시**" 못박음.
  - suite Step 1 업그레이드 라우팅 + **6개 스킬(data-model·ia-designer·design-concept·policy-legal·scenario·screen-design)
    gap-fill 절**에 동일 포인터 추가.

---

## [0.4.3] — 2026-07-20 (beta)

> 상태: `beta` 머지로 **버전 0.4.3 확정**, 릴리즈 날짜 = beta 머지일(2026-07-20).
> 설치 스크립트를 컨펌형으로 — 전역/프로젝트 선택을 대화로.

### Changed (변경)
- **`install.sh`·`install.ps1`: 인자 없이 실행하면 설치 위치를 컨펌한다.** "전역(~/.claude)에 설치할까요? [Y/n]"
  → Y/엔터=전역(`~/.claude/skills`), n=프로젝트 폴더 경로를 입력받아 `<폴더>/.claude/skills`에 설치.
  `-y`(ps1은 `-Yes`)면 컨펌 없이 전역. 위치 인자(`./install.sh <폴더>`·`-Base <폴더>`)는 그대로 비대화 설치(`.`=현재 폴더).
  비대화 입력(파이프 등)이면 컨펌을 건너뛰고 전역으로 폴백(안내 출력).
- README 설치 절을 컨펌 동작(+ `-y`)에 맞게 갱신.

---

## [0.4.2] — 2026-07-20 (beta)

> 상태: `beta` 머지로 **버전 0.4.2 확정**, 릴리즈 날짜 = beta 머지일(2026-07-20).
> 다운스트림(스킬 없는 소비자)용 점검·유지보수 키트 제공을 정식화.

### Added · Docs
- **suite: "점검·유지보수 키트 제공(요청 시·선제 제안)" 정식화** — 문서 세트를 이어받는 사람/에이전트가 easyproduct 없이도 점검·유지보수하도록, 요청/신호 시 `check-docs.mjs` + `checker-guide.md` + 동봉 스키마를 자족 키트로 제공. `skill-lint.mjs`는 제작자용(스킬 저장소 점검)이라 키트에서 **제외**(check-docs 포함 / skill-lint 제외 구분 명시).
- **checker-guide: "스킬 없이 이 문서 세트를 유지보수하려면" 절** 추가 — ssot 원본 기준 미러 재생성·빈 껍데기 금지·수정 후 `check-docs` 재실행·스키마 얼림(v2로만 변경).

---

## [0.4.1] — 2026-07-20 (beta)

> 상태: `beta` 머지로 **버전 0.4.1 확정**, 릴리즈 날짜 = beta 머지일(2026-07-20).
> 도그푸드가 관찰한 fresh 생성 지침의 본문 명문화(문서 견고성 — 기능 변경 없음).

### Docs (문서)
- **신규 생성 경로의 기계층 지침을 SKILL 본문에 명문화**(그동안 재진입 gap-fill에만 명시되고 fresh는 템플릿에 암묵 의존하던 비대칭 해소):
  - doc-builder: 신규 생성 시에도 각 문서 맨 앞 frontmatter(plan/design-doc/user-stories)를 둔다고 Step C에 명시.
  - data-model: "① 생성"에 "자동 점검할 수 있는 것" 절 포함을 명시.
  - scenario: 파생(S3) 생성 시 frontmatter 필드·자동점검 절 포함을 명시, 표준 형식 frontmatter 열거에 `status`를 추가(내부 불일치 정합).

---

## [0.4.0] — 2026-07-20 (beta)

> 상태: `beta` 머지로 **버전 0.4.0 확정**, 릴리즈 날짜 = beta 머지일(2026-07-20).
> 화면 설계 기계 블록 구조 재설계 + 스킬 부합 린트 신설(0.x에서 구조 변경 포함은 MINOR).

### Changed (변경)
- **화면 설계 `screendesign.screens`의 데이터 표현 재설계.** 평탄 `data:[…]`를 방향별로 나눠
  `data{ display(보여주는/읽기), io[{action,ui?,op?,sends[],receives[]}](동작별 보냄/받음), bindings[{ui,vars[]}](요소↔변수) }`로.
  손실 미러를 없애고 화면↔데이터·화면↔API(오퍼레이션) 계약 점검의 근거 마련(draft라 v1 제자리 정제).
  스키마·SKILL·suite Stage 4·`check-docs.mjs`·도메인 템플릿을 함께 갱신. `check-docs`는 구 평탄 `data` 호환.

### Added (추가)
- **스킬 부합 린트 `scripts/skill-lint.mjs`(무의존 Node).** 베타 머지 게이트의 기계 가능 부합 항목
  (빈 껍데기 금지·뜻 필수·정합성 점검 절 존재·점검기 자산)을 자동 점검, 실패 시 종료코드 1.

### Docs (문서)
- **8개 스킬 "정합성 점검" 절을 점검 3층 계약에 정렬**(누가·무엇을·언제 + ssot 원본 기준 재생성). `checker-guide`에 점검 3층 원칙·스킬 부합 게이트 명문화.
- **CLAUDE.md: "대대적 업그레이드 후 전체 흐름 도그푸드(원래 목적 회귀 점검)" 규칙** 추가. 이번 화면 data 재설계를 이 규칙대로 도그푸드해 통과 확인(신규 세트 생성 → check-docs·skill-lint 통과).

---

## [0.3.1] — 2026-07-18 (beta)

> 상태: `beta` 머지로 **버전 0.3.1 확정**, 릴리즈 날짜 = beta 머지일(2026-07-18).
> 0.3.0(기계 판독 층 도입)에 이어 같은 beta 날에 낸 보완·수정 릴리즈.

### Fixed (수정)
- **기계 블록 "빈 껍데기" 방지.** 블록의 존재 이유인 참조 필드가 비어도 검증을 통과하던 문제를 바로잡음:
  - `screendesign.screens`: `components`를 필수 + 최소 1개로. 실제 결함(화면 엔트리가 `{id, feat}`만 담고 산문의 컴포넌트·데이터를 전부 버림)을 고치고, 산문의 모든 `UI.*`·데이터 변수를 미러하도록 지침 강화.
  - `scenario.trace`: `refs`를 최소 1개로(아무것도 안 닿는 시나리오 금지 — 죽은 링크 점검 불가 방지).
  - `design.tokens`: 색뿐 아니라 `radius`·`spacing`·`type` 이름 척도까지 담도록 지침 강화(화면 설계의 `spacing.md`·`radius.md` 참조가 죽은 링크 되지 않게).

### Changed (변경)
- **공개·자기보고 표기를 스테이지 중립화.** 루트 `README.md`·각 `SKILL.md` 메타에서 `beta`/`main` 스테이지 라벨·문구 제거(버전·날짜 값만). 재발 방지 가드를 `VERSIONING.md` 원칙에 추가.

### Added (추가)
- 루트 `CLAUDE.md` — 이 저장소 작업 지침(기계 블록 설계 원칙 "빈 껍데기 금지" + 규약 포인터).
- `README.md`에 "소프트웨어·하네스가 읽고 점검하는 층" 안내 섹션 + 포지셔닝 문단(비개발자 생산·개발자/기계 소비, spec-kit 없이도 하네스 도입).

---

## [0.3.0] — 2026-07-18 (beta)

> 상태: `beta` 머지로 **버전 0.3.0 확정**, 릴리즈 날짜 = beta 머지일(2026-07-18).
> 릴리즈 날짜는 단일 필드이며, `main` 머지(정식 릴리즈) 시 그 날짜로 갱신된다.

### Added (추가)
- **문서 기계 판독 층 도입** — 각 스킬 산출 문서를 "사람용 + 기계용 2층"으로 확장. 소프트웨어(하네스·CI)가 규약으로 스펙을 조회·점검할 수 있다.
  - 문서 맨 앞 **frontmatter**(`doc_type`·`version`·`ssot`·`machine.*`)로 문서 종류와 기계 블록 위치·검증 계약을 선언.
  - 태그된 **기계 블록**: 데이터 모델 `datamodel.group`(`DATA.*`), IA `ia.features`(`FEAT.*`), 정책서 `policy.rules`(`POL.*`), 화면 설계 `screendesign.screens` + 인벤토리 `uicomponents.list`(`UI.*`), 디자인 컨셉 `design.tokens`, 시나리오 `scenario.trace`(`SCN.*`), 문서 묶음 색인 `docbundle.docs`(세트 매니페스트).
  - 기획서·설계서·유저 스토리는 상위 요구 문서라 **frontmatter(식별)만** 두고 기계 블록은 없음(그 내용의 ID는 하위 문서가 확정).
- **payload 스키마**(`schemas/*.v1.schema.json`) — 각 기계 블록의 JSON Schema 계약. 스킬 소유 고정 자산을 산출물 옆 `schemas/`에 버전 핀으로 복사.
- **크로스도큐먼트 참조 네임스페이스** — 접두사(`FEAT./DATA./POL./UI./SCN./token`)가 곧 "어느 등기부에서 조회할지"의 타입 표시. 문서 간 참조 무결성(죽은 링크)을 기계가 점검.
- **세트 전역 기계 점검 게이트**(suite Stage 4) — 시나리오 유무와 무관하게 각 문서의 스키마 검증 + 크로스도큐먼트 참조 무결성을 점검.
- **점검자 개발 가이드** `easyproduct-suite/references/checker-guide.md` — frontmatter 필드 의미·doc_type/anchor 레지스트리·참조 라우팅·점검자 알고리즘.
- **무의존 참조 점검기** `easyproduct-suite/scripts/check-docs.mjs`(Node) — suite가 문서 세트에 겨눠 실행. 프로젝트로 복사하지 않고 소스는 요청 시 제공.
- 각 스킬에 **옛 문서 gap-fill 업그레이드 경로** — frontmatter·기계 블록·스키마가 없는 기존 문서를 표·산문은 그대로 두고 기계층만 보강.

### Changed (변경)
- "봉투" 표기를 md 생태계 표준어 **frontmatter**로 통일(YAML 키·블록·스키마는 불변).

---

## [0.2.2] — 2026-07-15 (beta)

> 상태: `beta` 머지로 **버전 0.2.2 확정**, 릴리즈 날짜 = beta 머지일(2026-07-15).
> 릴리즈 날짜는 단일 필드이며, `main` 머지(정식 릴리즈) 시 그 날짜로 갱신된다.

### Changed (변경)
- **설치 스크립트가 버전을 표시** — `install.sh`/`install.ps1` 실행 시 **기존 설치 버전**과 **설치할(현재) 버전**을 알려준다(신규/재설치/갱신 구분). 버전은 각 스킬 `SKILL.md` 메타에서 읽는다(하드코딩 없음).
- `install.ps1`을 UTF-8 BOM으로 저장 + 버전 추출을 ASCII 정규식으로 변경 → Windows PowerShell 5.1에서도 한글 출력·버전 인식이 정상.

---

## [0.2.1] — 2026-07-15 (beta)

> 상태: `beta` 머지로 **버전 0.2.1 확정**, 릴리즈 날짜 = beta 머지일(2026-07-15).
> 릴리즈 날짜는 단일 필드이며, `main` 머지(정식 릴리즈) 시 그 날짜로 갱신된다.

### Added (추가)
- **설치 스크립트** `install.sh`(bash) · `install.ps1`(PowerShell) — 8개 스킬을 한 번에 전역(`~/.claude/skills`) 또는 지정 폴더(`<폴더>/.claude/skills`)에 설치/갱신. `.claude/skills`가 없으면 생성. README 설치 안내에 반영.

---

## [0.2.0] — 2026-07-15 (beta)

> 상태: `beta` 머지로 **버전 0.2.0 확정**, 릴리즈 날짜 = beta 머지일(2026-07-15).
> 릴리즈 날짜는 단일 필드이며, `main` 머지(정식 릴리즈) 시 그 날짜로 갱신된다.

### Added (추가)
- **data-model: 그룹별 인라인 JSON 미러** — 각 그룹 표 바로 아래에 그 구조를 담은 ```json 블록을 상시 포함(소프트웨어가 md 파싱 없이 구조를 읽도록). **표가 원본(SSOT), JSON은 재생성 미러**(손으로 고치지 않음).

### Changed (변경)
- **data-model: JSON 표기 통일** — 그룹/필드 오브젝트 스키마 + 영문 타입 enum(`string`/`number`/`datetime`…). 기존 "④ json 추출"을 **인라인 미러를 한 파일로 묶기**로 재정의하고, 결과 회신 구조체도 같은 스키마로 통일(이전의 상시-JSON 금지 원칙을 "표 SSOT + JSON 미러 재생성"으로 개정).
- **data-model 버전업** — 인라인 JSON이 없는 옛 문서에 표에서 **gap-fill 생성**하는 절차 추가, suite "오래된 구조" 트리거·데이터모델 재진입 라우팅에 반영.

---

## [0.1.0] — 2026-07-12 (beta)

> 상태: `beta` 머지로 **버전 0.1.0 확정**, 릴리즈 날짜 = beta 머지일(2026-07-12).
> 릴리즈 날짜는 단일 필드이며, `main` 머지(정식 릴리즈) 시 그 날짜로 갱신된다.

### Added (추가)
- **`easyproduct-scenario` 스킬 신설** — 사용자 여정 시나리오를 요구·설계·테스트를 잇는 "흐름의 축"으로 추가.
  - Given/When/Then + 스텝별 추적 링크 형식(`SCN.*` ID), 생애주기(스케치→이관→파생→승격→검증).
  - 두 거버넌스 규칙(결정 이관·링크-온리)으로 SSOT 보존.
  - 정합·추적 점검(죽은 링크는 확실히, 의미 모순은 후보만) 절차.
  - 필요 여정을 **기획서 핵심 행동·핵심 유스케이스에서 도출**하고, 커버리지 검사를 **필수**로(누락=테스트 구멍) 승격.
  - 시나리오 파일에 **mermaid flowchart 파생 뷰**(여정을 한눈에) 추가.
  - **게이트키퍼 역할**: 앞 게이트(Stage 1.5 스케치, 화면 전 조기 결함 발견)와 뒤 게이트(파생·검증)를 두고, 설계 결함은
    **회귀 후보와 함께 보고 후 사용자가 진행을 결정**(시나리오는 제기·라우팅 제안만, 자동 재설계 금지).
  - **작성 예시(`references/example-walkthrough.md`)** 추가: 채워진 시나리오(GWT+추적 링크+mermaid) + 게이트키퍼 보고의 실제 모습(참고 패턴 겸 확인용).
- **정책 규칙 참조용 안정 ID 체계 `POL.*`** — `easyproduct-policy-legal`에 도입.
  시나리오·화면 설계서·유스케이스가 정책 규칙을 이름이 아니라 ID로 참조 가능.
- **각 스킬 README.md** 추가.
- **버전·릴리즈 규칙 문서**(`VERSIONING.md`)와 이 `CHANGELOG.md` 추가.

### Fixed (검토 반영 — 논리적 구멍 6종)
- **유스케이스 안정 ID `UC.도메인.목표`** 도입(ia-designer) — 시나리오가 유스케이스를 이름이 아니라 ID로 참조(정책 `POL.*`·기능 `FEAT.*`와 대칭).
- **참조 표기 `이름(ID)`** 규칙 — 링크를 사람이 읽는 이름 + ID 함께(예: `메인 홈(FEAT.home.landing)`). 새로 만들거나 수정하는 문서에만 적용, 기존 문서 미적용은 결함 아님.
- **링크-온리 강화** — 예시·템플릿에서 정책 수치("24시간")를 문장에 복사하던 것 제거, 값은 원본만 두고 가리키기.
- **Stage 1.5 스케치 배선** — 스케치 산출물을 Stage 2(IA) 입력으로 흡수, 정책·데이터 결정은 뒤 Stage로(이관 타이밍 정정), handoff-guide 반영.
- **커버리지 "핵심 유스케이스" 정의 고정** — 기획 핵심행동(1순위) + IA 핵심흐름 파생 유스케이스.
- **문서 정합성** — 시나리오 도입 게이트를 "IA 이상"으로 확장, README 흐름도 순서(기능 관계도→시나리오)·명명 규칙(SCN.*/UC.*)·suite 설명 정정.

### Migration (기존 문서 업그레이드 경로)
- **옛 문서 세트 업그레이드 지원**: suite의 "오래된 구조" 감지에 `POL.*` ID 없음·시나리오 없음을 추가.
- policy-legal: 규칙 ID가 없는 기존 정책서에 **`POL.*` ID를 gap-fill로 부여**하는 버전업 절차 추가.
- scenario: 시나리오가 없던 옛 세트도 **기존 확정 문서에서 처음 파생**해 도입할 수 있음을 명시.

### Changed (변경)
- `easyproduct-suite`: 파생 시나리오 옵트인(3.8-D)과 Stage 4 시나리오 정합·추적 점검(3-1) 배선,
  색인·인계 템플릿에 시나리오 반영.
- `easyproduct-doc-builder`: GWT 배제 원칙이 비전문가 기획 단계 한정임을 명시(정형 시나리오는 scenario 스킬 소관).
- `easyproduct-ia-designer`: 유스케이스↔시나리오 경계, 읽기 전용 파생 원칙 공유 명시.
- `easyproduct-screen-design`: 시나리오가 있으면 화면 도출 드라이버로 사용(화면 SSOT는 글 정의).
- 루트 README: 스킬 7개 반영, 명명 규칙에 `POL.*` 추가.

---

## 이전 (버전 이전 · 초기 커밋)
- easyproduct 스킬 세트 최초 구성: 오케스트레이터 `easyproduct-suite` + 6개 전문 스킬, README·LICENSE.
