# 개발 문서 묶음 안내: [프로젝트 이름]

> **생성 시각: [YYYY-MM-DD]** 기준 스냅샷입니다.
> 이 문서는 그 시점에 프로젝트 폴더를 훑어 만든 **파생물**입니다 — SSOT가 아닙니다.
> 이후 각 문서가 개별적으로 수정·추가·삭제됐다면 **이 목록·경로·설명이 실제와 다를 수 있습니다.**
> 항상 옳은 것은 각 문서 자체이지 이 색인이 아닙니다. 최신 목록이 필요하면 "색인 다시 만들어줘"
> 라고 요청하세요(easyproduct-suite가 그 시점의 폴더를 다시 훑어 새로 씁니다).
> **이 파일은 `.gitignore`에 등록되어 있어 저장소에 커밋되지 않습니다.** 여러 사람 사이에서
> 오래된 스냅샷이 진실처럼 남는 걸 막기 위함입니다. 지울 필요는 없습니다 — 로컬에 남겨두고
> 필요할 때 열어 보셔도 됩니다.

> 이 문서는 [프로젝트 이름]을 만들기 위해 준비한 **문서 세트의 지도**입니다.
> 어떤 문서가 있고, 무엇을 위한 것이며, 어떤 순서로 읽으면 되는지 한 장에 정리했습니다.
> 각 문서는 언제든 따로 열어 고칠 수 있습니다 — 다만 위에 적었듯, 그렇게 고쳐도 **이 색인이
> 저절로 따라 갱신되지는 않습니다.**
> 이 문서 자체는 폴더 최상위(루트)의 `00-index.md`입니다.

## 폴더 구조 한눈에
```
[project-name]-docs/
├── 00-index.md                      ← 이 문서 (.gitignore 대상, 로컬엔 남음)
├── .gitignore                       ← "temp/" · "speckit-handoff-*.md" · "00-index.md"
├── ssot/                            ← 결정이 사는 곳(SSOT). 충돌 시 여기가 맞음
├── supporting/                      ← 보강자료(원본 아니지만 spec-kit이 실제로 참조)
├── reference/                       ← 파생이지만 보존(git 추적) — 버리지 않음
├── screens/{scope}/                 ← 화면 설계서(사용자 앱/백오피스 각각)
└── temp/mockups/{scope}/            ← 화면 목업. .gitignore 대상, 확인 후 버려도 됨(재생성 가능)
```
> spec-kit 인계 안내문은 이 트리에 없습니다 — 세트 폴더 구조 밖의 산출물로, 만들어서 바로
> 전달합니다(프로젝트 폴더 안에 정해진 자리를 두지 않습니다). 혹시 남더라도 `.gitignore`의
> `speckit-handoff-*.md` 패턴이 커밋을 막습니다.

## 한 줄 요약
누구를 위한 무엇인지, 어떤 종류의 서비스인지 한 문장.

## 이 세트에 들어 있는 문서

| 문서 | 무엇을 담나 | 파일 |
|---|---|---|
| **기획서** | 무엇을·왜 만드는지 (정체성·핵심 기능·규칙·성공 기준·정보 덩어리) | `ssot/plan.md` |
| **설계서** | 어떻게 만들지 (형태·규모·기술 방향·구성) | `ssot/design-spec.md` |
| **화면·기능 구조(IA)** | 어떤 화면·기능으로 이루어지는지 (프론트+관리자), FEAT ID 원본 | `ssot/ia.md` |
| **데이터 모델** | 다루는 정보의 그룹·필드·구조 — **이 문서(표)가 SSOT** | `ssot/data-model.md` |
| **디자인 컨셉** | 어떤 색·글꼴·느낌으로 보이는지, 선택된 안의 값이 SSOT | `ssot/design-concept.md` |

> 아래는 세트에서 **기본 포함**됩니다(개별 스킬로 따로 쓸 때는 요청 시에만). "빼줘"로 끈 경우에만 그 줄을 지우세요.

| (선택) **유저 스토리** | 누가·무엇을·왜 원하는지 목록 (+수용 기준). 기획서의 보강자료 | `supporting/user-stories.md` |
| (선택) **유스케이스** | 주요 목표별 흐름·예외·사후조건. 기획서·IA의 보강자료 | `supporting/use-cases.md` |
| (선택) **서비스 정책서** | 운영 규칙(회원·결제·환불·신고 등) | `ssot/policy.md` |
| (선택) **이용약관·개인정보처리방침** | 법적 문서 **초안·점검표** — ⚠️ 실제 사용 전 법률 전문가 검토 필요 | `ssot/terms-privacy.md` |

> 아래는 **옵트인**입니다(게이트에서 물어 진행). 만들었을 때만 이 줄을 남깁니다.

| (옵트인) **화면 설계서 — 사용자 앱** | 도메인별 파일 + 인덱스. 글 정의(SSOT)+데이터+목업(참고물), IA 사이트맵 순서 | `screens/user/screen-design-user-index.md` (+ 도메인별 파일) |
| (옵트인) **UI 컴포넌트 인벤토리 — 사용자 앱** | 재사용 컴포넌트 규격(SSOT). 화면마다 다시 정의하지 않고 이 문서를 참조 | `ssot/ui-components-user.md` |
| (옵트인) **화면 설계서 — 백오피스** | 관리자 화면 상세. 사용자 앱과 별개 프로젝트 문서(디자인은 절제된 변형) | `screens/backoffice/screen-design-backoffice-index.md` (+ 도메인별 파일) |
| (옵트인) **UI 컴포넌트 인벤토리 — 백오피스** | 절제된 변형이 적용된 별도 규격(SSOT) | `ssot/ui-components-backoffice.md` |
| (옵트인) **기능 관계도** | 프론트↔백오피스 연동(트리거) 관계. IA에서 도출한 **참조용 파생물**(원본은 IA) | `reference/feature-relation-map.md` |
| (옵트인) **spec-kit 인계 안내문 — 사용자 앱** | 이 세트를 spec-kit(SDD)에 넘길 인계장(사용자 앱 프로젝트분). **1회성 산출물 — 만들어 바로 전달, 세트 폴더 구조 밖** | `speckit-handoff-user.md` (전달됨) |
| (옵트인) **spec-kit 인계 안내문 — 백오피스** | 백오피스 프로젝트분 인계장(화면 설계서가 둘 다 있을 때만). 마찬가지로 전달만 하고 폴더에 편입 안 함 | `speckit-handoff-backoffice.md` (전달됨) |

> 디자인 3안 비교(HTML/PNG)는 `reference/design-preview/`에, 화면 목업(HTML)은 `temp/mockups/{scope}/`에 있습니다(둘 다 참조용·파생물이지만, 전자는 보존하고 후자는 버려도 됩니다 — 아래 "폴더 구조 한눈에" 참고).

## 읽는 순서 (권장)
1. **기획서** — 이 서비스가 무엇인지 먼저 잡는다.
2. **화면·기능 구조** — 그것이 어떤 화면·기능으로 나타나는지 본다.
3. **데이터 모델** — 그 화면들이 다룰 정보(그룹·필드)를 본다.
4. **디자인 컨셉** — 그 화면들을 어떤 겉모습으로 입힐지 본다.
5. **설계서** — 위를 아우른 "어떻게 만들지"를 확인한다. (앞 문서들을 요약해 가리킨다.)
6. (있으면) **화면 설계서** — 인덱스에서 도메인별 파일로 이동, IA 사이트맵 순서로 화면 확인(글 정의=표준, 목업=참고). UI 컴포넌트 인벤토리는 화면 안 `UI.*` ID의 규격 근거. 사용자 앱·백오피스는 별개 문서(별개 프로젝트).

## 지금 선택된 디자인 안
- **[선택된 안: 1안 / 2안 / 3안]** — [한 줄 이유]
- 미리보기: `reference/design-preview/design-comparison.html`(+png), 선택안 키트·샘플
- 다른 안으로 바꾸려면: `ssot/design-concept.md`의 "선택된 안"만 바꾸면 됩니다.

## 아직 안 정한 것 (세트 전체)
개발로 넘어가기 전 채우면 좋은, 아직 비어 있는 부분을 한데 모읍니다.
- [확인 필요: …]
- [확인 필요: …]

## 다음 단계
이 문서 세트는 그대로 개발에 사용할 수 있습니다. 내용이 바뀌면 `ssot/`의 해당 문서를 고치세요.
spec-kit(SDD)으로 개발하려면 spec-kit 인계 안내문(전달받았다면)을 spec-kit 세션 첫 입력으로
붙여넣으세요. (사용자 앱·백오피스가 별개 프로젝트라면 인계 안내문도 프로젝트별로 따로 붙여넣습니다.
이 문서는 1회성이라 프로젝트 폴더에는 남아있지 않습니다 — 다시 필요하면 재생성을 요청하세요.)
`temp/`는 버려도 되는 폴더입니다 — 지우거나 커밋하지 않아도, `ssot/`·`screens/`만 있으면
언제든 다시 만들 수 있습니다. (형식 변환·후속 작업은 개발 단계에서 진행됩니다.)
